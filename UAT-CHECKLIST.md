# UltiEssentials — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only, as
  `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
- **Layer**, copied verbatim from Laojun's own `ultitools-real-client-uat` skill so no translation
  step exists at dispatch time: `protocol`, `java-client`, `os-input`, `pixel`, `server`, `human`.
- **Human-authenticated-session rows (D-27b):** a row whose Steps can only be exercised through the
  maintainer's own authenticated UltiCloud panel session carries the fixed Preconditions phrase
  `maintainer-authenticated UltiCloud panel session (personal credentials)` and Layer `human`. This
  module has no panel-capability rows of its own, so no row below is currently affected; the
  convention is stated here for template consistency with the framework's checklist.
- **Expected** must name an observable truth — an exact chat line, a log line, a database row, an
  inventory slot — and never the words "it works". Per D-02 (English only) and the monorepo CJK
  scan, no row's Expected reproduces a literal Chinese character.
- **Covers** back-references a Phase 9 GUI-excluded class name; left blank when no such class
  applies. UltiEssentials is not one of the nine modules in Phase 9's GUI-exclusion register
  (confirmed by reading `.planning/phases/09-module-ecosystem-readiness-and-test-coverage/
  gui-exclusions/` directly — no `UltiEssentials.md` file exists there and no file in that
  directory names UltiEssentials), so every row below leaves `Covers` blank.
- A row whose Preconditions name a prior row must appear after that row in file order — asserted
  mechanically: for every row, every checklist ID cited in its Preconditions cell must have a
  strictly smaller line number in this file than the row citing it (sweep class 8, D-27a).
- **Config-per-file rule (D-06):** one checklist row per `@ConfigEntity`-annotated class, never one
  row per key. A config-per-file row's ID is suffixed `-yml` (e.g. `ultiessentials.config.
  essentials-yml`) and aggregates every per-key `ultiessentials.config.<file-stem>.*` row for that
  file rather than citing a single one of them.
- **Shipped default language is `zh`.** Every row below whose Expected quotes a literal in-game or
  console line carries the precondition `language: en` set in `plugins/UltiTools/config.yml`, so
  the observed line matches this document's English text exactly. Since this release
  (UltiKits/UltiEssentials#26) every chat, kick-screen and console line this module writes goes
  through its language catalogue; before it, most of them rendered Chinese whatever `language` said.
  For a session that checks text this release introduced or changed, move
  `plugins/UltiTools/pluginConfig/UltiEssentials/lang/` aside before the start so the jar's
  catalogues are extracted fresh (an upgraded install never refreshes an already-extracted
  language file, UltiKits/UltiTools-Reborn#459); see `ultiessentials.i18n.language`.
- **Two "teleport successful" lines exist for the same logical outcome, depending on path.**
  `TeleportService`'s warmup countdown sends `Teleport successful!` in green when a WARMUP
  completes; `HomeCommand`/`WarpCommand`'s own `switch` statements send `Teleport successful!`
  without the colour code for an INSTANT (no-warmup) teleport. Both variants are asserted below
  where relevant.

## Teleportation & World Presence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.spawn.teleport | `language: en`; `features.spawn.enabled: true` (shipped default); `config/spawn.yml` at its shipped default (`world: "world"`, a world that is loaded) | Run `/spawn` | The player is teleported to the configured spawn coordinates; chat shows the translated `Teleported to spawn` line (`SpawnCommand.java:39` -- that specific key IS present in `lang/en.json`, unlike this command's own `handleHelp` text) | server | |
| ultiessentials.spawn.teleport.neg-disabled | Same as `ultiessentials.spawn.teleport`, except `features.spawn.enabled: false` | Run `/spawn` | Chat shows the translated `This feature is disabled` line, with no colour code (`SpawnCommand.java:28` looks up the Chinese-sentence key that `lang/en.json` maps to this text); the player is not moved | server | |
| ultiessentials.spawn.set | `language: en`; `features.spawn.enabled: true` | Stand at a distinguishable position, run `/setspawn`, then restart the server and run `/spawn` | Chat shows the translated `Spawn point set` line (`SetSpawnCommand.java:36` -- translated); the later `/spawn` teleports to the SAME position, confirming `config/spawn.yml` was written to disk, not merely held in memory | server | |
| ultiessentials.spawn.on-respawn | `features.spawn.enabled: true`; `spawn.teleport-on-respawn: true` (shipped default); the player's normal bed/anchor respawn point is somewhere OTHER than the configured spawn location | Die and respawn | The player respawns at the configured spawn coordinates, not their bed/anchor respawn point | server | |
| ultiessentials.spawn.on-respawn.neg-disabled | Same as `ultiessentials.spawn.on-respawn`, except `spawn.teleport-on-respawn: false` | Die and respawn | The player respawns at their normal bed/anchor respawn point (or the world spawn if none set), unaffected by `config/spawn.yml` | server | |
| ultiessentials.spawn.on-first-join | `features.spawn.enabled: true`; `spawn.teleport-on-first-join: true` (shipped default); a player UUID that has genuinely never joined this server before (`Player#hasPlayedBefore()` false) | The player joins for the first time | The player is teleported to the configured spawn coordinates immediately on join | server | |
| ultiessentials.lobby.teleport | `language: en`; `features.lobby.enabled: true` (shipped default); `config/lobby.yml` at its shipped default | Run `/lobby` | The player is teleported to the configured lobby coordinates; chat shows the translated `Teleported to lobby` line (`LobbyCommand.java:39` -- translated) | server | |
| ultiessentials.lobby.teleport.neg-disabled | Same as `ultiessentials.lobby.teleport`, except `features.lobby.enabled: false` | Run `/hub` (alias) | Chat shows the translated `This feature is disabled` line, with no colour code (`LobbyCommand.java:28`, the same key as `ultiessentials.spawn.teleport.neg-disabled`); the player is not moved | server | |
| ultiessentials.lobby.set | `language: en`; `features.lobby.enabled: true` | Stand at a distinguishable position, run `/setlobby`, then restart the server and run `/lobby` | Chat shows the translated `Lobby set` line (`SetLobbyCommand.java:36` -- translated); the later `/lobby` teleports to the SAME position, confirming `config/lobby.yml` was written to disk | server | |
| ultiessentials.wild.teleport | `language: en`; `features.wild.enabled: true`; `features.wild.min-range: 100`, `features.wild.max-range: 10000` (shipped defaults); the player is not currently on `/wild`'s cooldown | 1. Note the player's position, then run `/wild`, and do not move the player afterwards. 2. From the server console, with `X`, `Y` and `Z` the three numbers in the `Randomly teleported to` line, probe the floor block: `execute if block X Y Z #minecraft:replaceable`. 3. For the feet block (height `Y`+1) and the head block (height `Y`+2) in the same column, run each of `execute if block X <height> Z #minecraft:replaceable`, `execute if block X <height> Z minecraft:water` and `execute if block X <height> Z minecraft:lava` | Chat first shows the translated `Finding a safe location...` line (`WildCommand.java:53` -- translated), then the translated `Randomly teleported to X:<X> Y:<Y> Z:<Z>` line (`WildCommand.java:68` -- translated). **The three numbers name the floor block the player lands on, not the player's own position:** `X` and `Z` are that block's column, `Y` is the height of the highest block in it (`world.getHighestBlockYAt(x, z)`, `WildCommand.java:63`; on Paper 1.21.11 the top block of the `MOTION_BLOCKING` heightmap), and the player's feet are placed one block higher, at the centre of that column (`new Location(world, x + 0.5, y + 1, z + 0.5)`, `WildCommand.java:64`). A `Y` one below the player's feet is therefore correct (observed on a real server: `Y:62` in chat, feet at `63.0`). Record `pass` only when all four hold: (a) the teleport the client is sent -- or, before the player moves, `data get entity <player> Pos` from the console -- puts the player at exactly (`X`+0.5, `Y`+1.0, `Z`+0.5); (b) that point's horizontal distance from the position noted in step 1 is at least 97 and at most 10002 blocks (the random radius is 100 to 9999, `WildCommand.java:57`, and snapping the point to a block centre, `WildCommand.java:60-64`, moves it by at most about 2.1 blocks); (c) the floor probe prints `Test failed`: the floor is not air, water, lava, or a passable plant or snow layer, which are the members of `#minecraft:replaceable` (read them from the server jar's `data/minecraft/tags/block/replaceable.json`); (d) the feet block and the head block each print `Test passed` for `#minecraft:replaceable` and `Test failed` for water and for lava. Record `fail` if (a), (b) or (c) does not hold. Record `blocked`, never `pass`, if the translated `Could not find a safe location, please try again later` line (`WildCommand.java:73` -- translated) appears instead (all ten candidate points were rejected, as can happen over open water; the player must not have moved, and the row is re-run); if the feet or head block fails (d) -- a flower, a sapling or a similar block that `isSafeLocation` (`WildCommand.java:82-103`) accepts as not solid but this probe cannot classify, or a liquid; or if any read-back is missing | server | |
| ultiessentials.wild.teleport.neg-cooldown | Same as `ultiessentials.wild.teleport`, with `features.wild.cooldown: 60` (shipped default), run immediately again within 60 seconds of the prior `/wild` | Run `/wild` a second time inside 60 seconds | The command is rejected by the framework's own `@CmdCD` cooldown validator (a cooldown-remaining message with a count of at most 60 seconds), NOT by `WildCommand`'s own logic; the player is not moved. The cooldown is `features.wild.cooldown` (UltiKits/UltiEssentials#27) | server | |
| ultiessentials.wild.teleport.neg-cooldown-configured | Same as `ultiessentials.wild.teleport`, except `features.wild.cooldown: 10` (NOT the shipped default) in `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`, applied with `ul reload UltiEssentials` from the console | 1. Run `/wild`. 2. Immediately run `/wild` again. 3. Wait 11 seconds, then run `/wild` a third time. 4. Cleanup: set `features.wild.cooldown: 60` and run `ul reload UltiEssentials` | Step 2 is refused by the framework's cooldown message with a remaining count of at most 10 seconds — not about 60, which is what a cooldown that ignored the key would show; step 3 is accepted (the `Finding a safe location...` line appears again). Before UltiKits/UltiEssentials#27 the cooldown was 60 seconds whatever the key said | server | |
| ultiessentials.wild.teleport.neg-cooldown-zero | Same as `ultiessentials.wild.teleport`, with `features.wild.cooldown: 60` (shipped default) at first; a first and a second player, both holding `ultiessentials.wild` and neither on the cooldown | 1. Control: the first player runs `/wild`, then immediately `/wild` again. 2. Set `features.wild.cooldown: 0` and run `ul reload UltiEssentials`. 3. The second player runs `/wild`, then immediately `/wild` again. 4. Within 60 seconds of step 1, the first player runs `/wild` once more. 5. Cleanup: set `features.wild.cooldown: 60` and run `ul reload UltiEssentials` | Step 1's second `/wild` is refused with the cooldown message — the control that `/wild` has a cooldown at all; if it is accepted, record the row `blocked`. Step 3's second `/wild` is accepted with no cooldown message: the `Finding a safe location...` line appears again, because `0` means no cooldown. Step 4 is still refused: a cooldown already running keeps its end time when the setting becomes `0` (UltiKits/UltiEssentials#27) | server | |
| ultiessentials.wild.teleport.neg-cooldown-reload | Same as `ultiessentials.wild.teleport`, with `features.wild.cooldown: 120` (NOT the shipped default) applied with `ul reload UltiEssentials`; a SECOND player holding `ultiessentials.wild`, also not on the cooldown | 1. The first player runs `/wild`. 2. Set `features.wild.cooldown: 5` and run `ul reload UltiEssentials`. 3. Within 100 seconds of step 1 (well inside its 120-second cooldown), the first player runs `/wild` again. 4. The second player runs `/wild`, then immediately `/wild` again. 5. Set `features.wild.cooldown: -1` together with `features.wild.max-range: 20000`, run `ul reload UltiEssentials`, and read the console. 6. The second player waits 6 seconds, runs `/wild`, then immediately `/wild` again. 7. Cleanup: set `features.wild.cooldown: 60` and `features.wild.max-range: 10000`, and run `ul reload UltiEssentials` | Step 3 is refused with a remaining count above 5 seconds: a cooldown already running keeps the end time it was stamped with (if more than 115 seconds passed since step 1, record `blocked`). Step 4's second `/wild` is refused with a remaining count of at most 5 seconds: the reloaded value applies to the next use. Step 5: the console shows a WARNING naming `features.wild.cooldown` that it was not updated and keeps 5 seconds, AND the framework's `Module 'UltiEssentials' reloaded.` line — the rest of the reload completed. Step 6's second `/wild` is again refused with at most 5 seconds remaining: the negative value never took effect (UltiKits/UltiEssentials#27) | server | |
| ultiessentials.back.teleport | `language: en`; `features.back.enabled: true` (shipped default); the player has run at least one COMMAND- or PLUGIN-caused teleport since their last `/back` (run `ultiessentials.spawn.teleport` first) | Run `/back` | The player is teleported to the position they were at immediately before the tracked teleport; chat shows the translated `Teleported to previous location` line (`BackCommand.java:56` -- translated) | server | |
| ultiessentials.back.teleport.neg-no-location | `language: en`; `features.back.enabled: true`; a freshly-joined player who has never triggered a COMMAND/PLUGIN teleport this session | Run `/back` | Chat shows the translated `No location to return to` line (`BackCommand.java:51` -- translated); the player is not moved | server | |
| ultiessentials.back.track | `features.back.enabled: true` | Run `ultiessentials.spawn.teleport`, noting the exact position beforehand, then immediately run `ultiessentials.back.teleport` | The player returns to the EXACT position recorded before the `/spawn` teleport (not merely "near" it) — confirms the `PLUGIN`-cause teleport this plugin's own `/spawn` performs is tracked, not only vanilla `COMMAND`-cause teleports | server | |
| ultiessentials.back.cleanup-on-quit | `features.back.enabled: true`; the player has a recorded back-location (run `ultiessentials.back.track` first) | Quit and rejoin the server, then run `/back` | Chat shows the same translated `No location to return to` line as `ultiessentials.back.teleport.neg-no-location` -- the recorded location did not survive the quit | server | |

## Homes

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.home.set-default | `language: en`; `features.home.enabled: true` (shipped default); the player has no home named `home` yet | Run `/sethome` | Chat shows `Home set! (home)` then `Current homes: 1/3` (shipped `home.default-max-homes`) | server | |
| ultiessentials.home.set-named | `language: en`; `features.home.enabled: true`; the player has no home named `farm` yet, and holds fewer than 3 homes total (run `ultiessentials.home.set-default` first -- `/sethome farm` alone does not also create a `home`-named entry) | Run `/sethome farm` | Chat shows the same "home set" success line naming `farm`; `/homes` (see `ultiessentials.home.list`) now lists both `home` and `farm` | server | |
| ultiessentials.home.set-named.neg-limit | `language: en`; `features.home.enabled: true`; the player already holds exactly `home.default-max-homes` (3, shipped default) DISTINCT home names and holds no `ultiessentials.home.max.*`/`.unlimited` permission (run `ultiessentials.home.set-default` and `ultiessentials.home.set-named` first, then set one more distinctly-named home to reach 3) | Run `/sethome fourth` | Chat shows `You have reached the maximum number of homes` immediately followed by `Use /delhome <name> to delete a home`; no fourth home is created — `/homes` still lists exactly 3 | server | |
| ultiessentials.home.teleport-default | `language: en`; `features.home.enabled: true`; a home named `home` exists (run `ultiessentials.home.set-default` first); `features.home.teleport-warmup: 3` (shipped default); the player does NOT hold `ultiessentials.home.nowarmup` | Run `/home` | Chat first shows `Teleporting soon, please do not move...`, then one `Teleporting in <n>s, please do not move...` line per second of the countdown (yellow), then the player is teleported to the `home` location and chat shows `Teleport successful!` (green, sent by `TeleportService` when the warmup completes) | server | |
| ultiessentials.home.teleport-default.neg-nowarmup | Same as `ultiessentials.home.teleport-default`, except the player holds `ultiessentials.home.nowarmup` | Run `/home` | The player is teleported to `home` INSTANTLY, with no warmup countdown; chat shows `Teleport successful!` without the green colour code the warmup path uses (see the Conventions note on the two success lines) | server | |
| ultiessentials.home.teleport-named | `language: en`; `features.home.enabled: true`; a home named `farm` exists (run `ultiessentials.home.set-named` first) | Run `/home farm` | The player is teleported to the `farm` location, with the same warmup/instant success-message split as `ultiessentials.home.teleport-default` depending on `ultiessentials.home.nowarmup` | server | |
| ultiessentials.home.teleport-default.neg-none-set | `language: en`; `features.home.enabled: true`; a player with zero homes set | Run `/home` | Chat shows `You have not set any homes`; the player is not moved | server | |
| ultiessentials.home.teleport-named.neg-not-found | `language: en`; `features.home.enabled: true`; no home named `does-not-exist` exists for the player | Run `/home does-not-exist` | Chat shows `Home not found`; the player is not moved | server | |
| ultiessentials.home.delete | `language: en`; `features.home.enabled: true`; a home named `farm` exists (run `ultiessentials.home.set-named` first) | Run `/delhome farm` | Chat shows `Home deleted! (farm)`; `/homes` (see `ultiessentials.home.list`) no longer lists `farm`; running `/delhome farm` a second time now reports `Home not found` instead of a second success | server | |
| ultiessentials.home.delete.neg-not-found | `language: en`; `features.home.enabled: true`; no home named `does-not-exist` exists | Run `/delhome does-not-exist` | Chat shows `Home not found` | server | |
| ultiessentials.home.list | `language: en`; `features.home.enabled: true`; the player holds at least one home (run `ultiessentials.home.set-default` first) | Run `/homes` | Chat shows a gold header line `========== Your homes (1/3) ==========`, then one line per home showing its name, world, and integer-truncated coordinates, then a closing `======...` divider | server | |
| ultiessentials.home.list.neg-empty | `language: en`; `features.home.enabled: true`; a player holding zero homes | Run `/homes` | The header line still shows `0/3`, followed immediately by `You have not set any homes` and `Use /sethome <name> to set a home` (both gray) — no home entry lines | server | |

## Warps

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.warp.create | `language: en`; `features.warp.enabled: true` (shipped default); no warp named `shop` exists yet | Run `/setwarp shop` | Chat shows `Created warp: shop` | server | |
| ultiessentials.warp.create.neg-exists | `language: en`; `features.warp.enabled: true`; a warp named `shop` already exists (run `ultiessentials.warp.create` first) | Run `/setwarp shop` again | Chat shows `Warp already exists: shop`; the ORIGINAL warp's coordinates are unchanged (confirm via `/warp shop`) | server | |
| ultiessentials.warp.create-with-permission | `language: en`; `features.warp.enabled: true`; no warp named `vip` exists yet | Run `/setwarp vip vip.warp.access` | Chat shows `Created warp: vip (permission: vip.warp.access)` on one line | server | |
| ultiessentials.warp.teleport | `language: en`; `features.warp.enabled: true`; a warp named `shop` exists with no access permission (run `ultiessentials.warp.create` first); `features.warp.teleport-warmup: 3` (shipped default); the player does NOT hold `ultiessentials.warp.nowarmup` | Run `/warp shop` | Chat first shows `Teleporting, please do not move...`, then the countdown lines `Teleporting in <n>s, please do not move...`, then the player is teleported to `shop`'s coordinates with `Teleport successful!` (green) — same warmup/instant success-message split noted in `## Homes` | server | |
| ultiessentials.warp.teleport.neg-no-permission | `language: en`; `features.warp.enabled: true`; a warp named `vip` exists requiring `vip.warp.access` (run `ultiessentials.warp.create-with-permission` first); the player does NOT hold `vip.warp.access` | Run `/warp vip` | Chat shows `You do not have permission to use this warp`; the player is not moved | server | |
| ultiessentials.warp.teleport.neg-not-found | `language: en`; `features.warp.enabled: true`; no warp named `does-not-exist` | Run `/warp does-not-exist` | Chat shows `Warp not found: does-not-exist` | server | |
| ultiessentials.warp.delete | `language: en`; `features.warp.enabled: true`; a warp named `shop` exists (run `ultiessentials.warp.create` first) | Run `/delwarp shop` | Chat shows `Deleted warp: shop`; `/warps` (see `ultiessentials.warp.list`) no longer lists `shop`; running `/delwarp shop` a second time now reports `Warp not found: shop` instead of a second success | server | |
| ultiessentials.warp.delete.neg-not-found | `language: en`; `features.warp.enabled: true`; no warp named `does-not-exist` | Run `/delwarp does-not-exist` | Chat shows `Warp not found: does-not-exist` | server | |
| ultiessentials.warp.list | `language: en`; `features.warp.enabled: true`; a warp named `shop` (no access permission) exists (run `ultiessentials.warp.create` first); a second warp named `vip` requiring `vip.warp.access` exists (run `ultiessentials.warp.create-with-permission` first); the listing player does NOT hold `vip.warp.access` | Run `/warps` | Chat shows the gold header `=== Warp List ===`, then lists `shop` with world and one-decimal coordinates but NOT `vip` (the player lacks its access permission), then the footer `Total: 1 available warps` (gray) | server | |
| ultiessentials.warp.list.neg-empty | `language: en`; `features.warp.enabled: true`; zero warps accessible to the listing player | Run `/warps` | Chat shows only `No warps available` — no header, no entries | server | |

## TPA (Teleport Ask)

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.tpa.send | `language: en`; `features.tpa.enabled: true` (shipped default); the sender is not on the `features.tpa.cooldown` (shipped default 10s); the target is online in the same world (or `features.tpa.allow-cross-world: true`); the target holds no other pending request | Run `/tpa <target>` | The sender sees `Teleport request sent to <target>` then `Waiting for them to accept...`; the target sees `<sender> wants to teleport to you` then `Use /tpaccept to accept or /tpdeny to deny` | server | |
| ultiessentials.tpa.send.neg-self | `language: en`; `features.tpa.enabled: true` | Run `/tpa <own-username>` | Chat shows `You cannot send a teleport request to yourself`; no request is created (confirm via `/tpaccept` reporting `No pending teleport request`) | server | |
| ultiessentials.tpa.send.neg-target-busy | `language: en`; `features.tpa.enabled: true`; the target already holds a pending request from a third player (run `ultiessentials.tpa.send` from a different sender to the same target first) | A second sender runs `/tpa <same-target>` | The second sender sees `That player already has a pending teleport request`; the target's original pending request (from the first sender) is unchanged | server | |
| ultiessentials.tpa.send.neg-cooldown | `language: en`; `features.tpa.enabled: true`; `features.tpa.cooldown: 10` (shipped default); the sender sent a request within the last 10 seconds (run `ultiessentials.tpa.send` immediately before) | The same sender runs `/tpa <a-different-target>` immediately | Chat shows `Please wait before sending another request (<n>s)` with the remaining seconds | server | |
| ultiessentials.tpa.send.neg-cross-world | `language: en`; `features.tpa.enabled: true`; `features.tpa.allow-cross-world: false` (NOT the shipped default — flip it for this row); sender and target in different worlds | Run `/tpa <target>` | Chat shows `Teleporting across worlds is not allowed`; no request is created | server | |
| ultiessentials.tpa.send-here | `language: en`; `features.tpa.enabled: true`; same cooldown/busy/world preconditions as `ultiessentials.tpa.send` | Run `/tpahere <target>` | The sender sees the same `Teleport request sent to <target>` / `Waiting for them to accept...` pair as `ultiessentials.tpa.send`; the target sees `<sender> wants you to teleport to them` then the same `/tpaccept`/`/tpdeny` hint | server | |
| ultiessentials.tpa.accept | `language: en`; `features.tpa.enabled: true`; the accepting player holds a pending `/tpa` request from an online sender (run `ultiessentials.tpa.send` first) | The target runs `/tpaccept` | The ORIGINAL SENDER (not the accepter) is teleported to the accepter's location; the accepter sees `Teleport request accepted`; the sender sees `<accepter> accepted your teleport request` | server | |
| ultiessentials.tpa.accept.neg-none | `language: en`; `features.tpa.enabled: true`; the player holds no pending request | Run `/tpaccept` | Chat shows `No pending teleport request`; no teleport occurs | server | |
| ultiessentials.tpa.accept.neg-here | `language: en`; `features.tpa.enabled: true`; the accepting player holds a pending `/tpahere` request instead of `/tpa` (run `ultiessentials.tpa.send-here` first) | The target runs `/tpaccept` | The ACCEPTER (not the original sender) is teleported to the sender's location — the opposite direction from `ultiessentials.tpa.accept`, confirming `TpaType.TPA_HERE` is honoured | server | |
| ultiessentials.tpa.deny | `language: en`; `features.tpa.enabled: true`; the denying player holds a pending request from an online sender (run `ultiessentials.tpa.send` first) | The target runs `/tpdeny` | The denying player sees `Teleport request denied`; the original sender sees `Your teleport request was denied`; no teleport occurs for either player | server | |
| ultiessentials.tpa.deny.neg-none | `language: en`; `features.tpa.enabled: true`; the player holds no pending request | Run `/tpdeny` | Chat shows the same "no pending request" line as `ultiessentials.tpa.accept.neg-none` (read `TpDenyCommand.java:39`) | server | |
| ultiessentials.tpa.clear-on-quit | `language: en`; `features.tpa.enabled: true` and `features.tpa.timeout: 30` (both shipped defaults); three online players in the same world — a first sender, a target and a second sender — none of them on the `features.tpa.cooldown`; the target holds no pending request | Sender quits: 1. The first sender runs `/tpa <target>`, exactly as in `ultiessentials.tpa.send`. 2. Within 5 seconds, the first sender quits the server. 3. Within 10 seconds of step 1 — well inside the 30-second timeout — the second sender runs `/tpa <target>`. 4. The target runs `/tpaccept`. Target quits: 5. The first sender rejoins. At least 10 seconds after step 3 (so the second sender is off the cooldown), the second sender runs `/tpa <target>` again. 6. Within 5 seconds, the TARGET quits and then rejoins at once. 7. Within 20 seconds of step 5 — still inside that request's 30-second timeout — the first sender runs `/tpa <target>`. 8. The target runs `/tpaccept`. | Step 3 is accepted: the second sender sees the same "request sent" line as `ultiessentials.tpa.send` (`TpaCommand.java:50`), NOT the "target has a pending request" line of `ultiessentials.tpa.send.neg-target-busy` (`TpaCommand.java:59`), and the target sees the "wants to teleport to you" line prefixed with the SECOND sender's name. Step 4 teleports the second sender to the target — the first sender's request was cleared when they quit rather than when it would have timed out. Step 7 is likewise accepted with the "request sent" line, not the busy line, and step 8 teleports the FIRST sender — the second sender's request was cleared when the target quit. If step 3 or step 7 shows the busy line, record `failed` (UltiKits/UltiEssentials#30) | server | |

## Player Status & Utility

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.heal.self | `language: en`; `features.heal.enabled: true` (shipped default); the player's health is below max | Run `/heal` | The player's health is restored to their max-health attribute value; chat shows the translated `Health restored` line (read `lang/en.json` for the exact key/text pair) | server | |
| ultiessentials.heal.other | `language: en`; `features.heal.enabled: true`; an online target below max health; the sender holds `ultiessentials.heal.other` | Run `/heal <target>` | The target's health is restored to max; the sender sees a translated "Restored `<target>`'s health" line; the target separately receives a translated "Your health has been restored" notification | server | |
| ultiessentials.heal.other.neg-offline | `language: en`; `features.heal.enabled: true`; no player named `does-not-exist` is online | Run `/heal does-not-exist` | Chat shows the translated `Player not found or offline` line | server | |
| ultiessentials.feed.self | `language: en`; `features.heal.enabled: true`; the player's food level is below 20 | Run `/feed` | The player's food level is set to 20 and saturation to 20.0; chat shows the translated `Hunger restored` line | server | |
| ultiessentials.feed.other | `language: en`; `features.heal.enabled: true`; an online target below full food; the sender holds `ultiessentials.heal.other` (shared with `/heal`, see FEATURES.md section note) | Run `/feed <target>` | The target's food is set to 20/saturation 20.0; the sender and target each receive the translated feed-restored equivalents of `ultiessentials.heal.other`'s messages | server | |
| ultiessentials.speed.set | `language: en`; `features.speed.enabled: true` (shipped default); `features.speed.max-speed: 10` (shipped default) | Run `/speed 5` | The player's walk speed becomes `0.2 * 5 = 1.0` (capped at 1.0) and fly speed `min(1.0, 0.1*5)=0.5`; chat shows the translated `Speed set to 5` line | server | |
| ultiessentials.speed.set.neg-out-of-range | `language: en`; `features.speed.enabled: true`; `features.speed.max-speed: 10` | Run `/speed 11` | Chat shows the translated `Speed must be between 0-10` line; the player's speed is unchanged | server | |
| ultiessentials.speed.reset | `language: en`; `features.speed.enabled: true`; the player's speed was previously changed (run `ultiessentials.speed.set` first) | Run `/speed reset` | The player's walk speed returns to 0.2 and fly speed to 0.1 (the platform default); chat shows the translated `Speed reset to default` line | server | |
| ultiessentials.fly.toggle-self | `language: en`; `features.fly.enabled: true` (shipped default); the player's flight is currently disallowed | Run `/fly` | `Player#getAllowFlight()` becomes true; chat shows the translated `Flight mode enabled` line | server | |
| ultiessentials.fly.toggle-self.neg-disable | Same as `ultiessentials.fly.toggle-self`, run again with flight currently allowed | Run `/fly` again | `Player#getAllowFlight()` becomes false and the player is also forced out of active flight (`setFlying(false)`); chat shows the translated `Flight mode disabled` line | server | |
| ultiessentials.fly.toggle-other.neg-self-node-only | `language: en`; `features.fly.enabled: true`; an online target; the acting player is a non-operator holding `ultiessentials.fly` and NOT `ultiessentials.fly.other` | The acting player runs `/fly <target>` | The command is refused with the framework's no-permission response and the target's flight allowance is unchanged — ordinary self-fly access no longer carries flight control over other players (UltiKits/UltiEssentials#25) | server | |
| ultiessentials.fly.toggle-other | `language: en`; `features.fly.enabled: true`; an online target; the acting player holds BOTH `ultiessentials.fly` and `ultiessentials.fly.other` | The acting player runs `/fly <target>` | The target's flight allowance is toggled and both players receive the translated notification pair | server | |
| ultiessentials.hide.toggle | `language: en`; `features.hide.enabled: true` (shipped default); the player is currently visible; at least one other online player who does NOT hold `ultiessentials.hide.see` | Run `/hide` | The sender becomes invisible to the other online player (no longer appears in their tab list / is not visible in-world); chat shows the translated `Vanish mode enabled` line | server | |
| ultiessentials.hide.toggle.neg-see-permission | Same as `ultiessentials.hide.toggle`, except the OTHER online player holds `ultiessentials.hide.see` | The vanished player is checked against this second player | The second player CAN still see the vanished player (visibility exemption honoured per-observer) | server | |
| ultiessentials.hide.reapply-on-join | `language: en`; `features.hide.enabled: true` (shipped default); a first player is vanished (run `ultiessentials.hide.toggle` first) and a second online player is NOT vanished; a third player, who does NOT hold `ultiessentials.hide.see`, is offline | 1. The third player joins the server. 2. The third player opens the tab list and looks around the first player's position | The third player's tab list does not list the first (vanished) player and the first player is not visible in-world, while the second (not vanished) player IS listed — that control shows the joining client's player list is being read at all. Before UltiKits/UltiEssentials#32 the vanished player was listed and visible to anyone who joined after the `/hide`. If the second player is not listed either, record the row `blocked`, not `passed` | java-client | |
| ultiessentials.hide.reapply-on-join.neg-see-permission | Same as `ultiessentials.hide.reapply-on-join`, except the joining third player DOES hold `ultiessentials.hide.see` | Same steps as `ultiessentials.hide.reapply-on-join` | The third player's tab list lists the first (vanished) player and shows them in-world, as `ultiessentials.hide.toggle.neg-see-permission` does for a player who was already online when the first player vanished | java-client | |
| ultiessentials.gamemode.set-self | `language: en`; `features.gamemode.enabled: true` (shipped default) | Run `/gm creative` | The player's game mode becomes CREATIVE; chat shows the translated `Game mode changed to CREATIVE` line | server | |
| ultiessentials.gamemode.set-self.neg-invalid | `language: en`; `features.gamemode.enabled: true` | Run `/gm bogus` | Chat shows the translated `Invalid game mode. Available: 0/s/survival, 1/c/creative, 2/a/adventure, 3/sp/spectator` line; the player's game mode is unchanged | server | |
| ultiessentials.gamemode.set-other | `language: en`; `features.gamemode.enabled: true`; an online target; the sender holds `ultiessentials.gamemode.other` | Run `/gm survival <target>` | The target's game mode becomes SURVIVAL; the sender sees a translated "Changed `<target>`'s game mode to SURVIVAL" line; the target separately sees a translated "Your game mode has been changed to SURVIVAL" line | server | |
| ultiessentials.gamemode.shortcut-creative | `language: en`; `features.gamemode.enabled: true` | Run `/gmc` | The player's game mode becomes CREATIVE; chat shows the same translated line as `ultiessentials.gamemode.set-self` | server | |
| ultiessentials.gamemode.shortcut-spectator | `language: en`; `features.gamemode.enabled: true` | Run `/gmsp` | The player's game mode becomes SPECTATOR; chat shows the translated `Game mode changed to SPECTATOR` line | server | |
| ultiessentials.gamemode.shortcut-survival | `language: en`; `features.gamemode.enabled: true` | Run `/gms` | The player's game mode becomes SURVIVAL; chat shows the translated `Game mode changed to SURVIVAL` line | server | |
| ultiessentials.scoreboard.toggle | `language: en`; the player's sidebar scoreboard is currently off; `features.scoreboard.title` and `features.scoreboard.lines` at their shipped defaults, written into `config/essentials.yml` in English at module start (`## Config text materializer` below) | Run `/scoreboard` | The player's sidebar scoreboard appears, titled `Server Info` and showing the ten default lines (the first `Welcome, <player>`), exactly `features.scoreboard.title` and `features.scoreboard.lines` as the file holds them, or the configured title and lines if customised; chat also shows `Scoreboard enabled` (green) | server | |
| ultiessentials.scoreboard.enable | `language: en`; the player's sidebar scoreboard is currently off | Run `/scoreboard on` | The scoreboard appears; chat shows `Scoreboard enabled` (green), as in `ultiessentials.scoreboard.toggle` | server | |
| ultiessentials.scoreboard.enable.neg-already | Same as `ultiessentials.scoreboard.enable`, run again while already on | Run `/scoreboard on` a second time | The scoreboard remains unchanged; chat shows `The scoreboard is already enabled` (yellow) rather than the enable-success line | server | |
| ultiessentials.scoreboard.disable | `language: en`; in `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`, BEFORE the server's last restart: `features.nameprefix.enabled: true` (NOT the shipped `false`), `features.nameprefix.prefix-format: "&7[UAT] "` (NOT the shipped default: a literal with no placeholder) and `features.scoreboard.enabled: true` (shipped default); before that restart, `team list` from the console listed no team whose name starts with `up_`; the player joined after that restart, has been online for at least 5 seconds and is the only player who has joined since it (so there is one `up_` team); the player's sidebar scoreboard is currently on (run `ultiessentials.scoreboard.enable` first); `team` and `ul` run from the server console | 1. From the console run `team list`, then `team list` on the `up_` team it shows; as the player look at the screen and open the tab list. 2. As the player run `/scoreboard off`. 3. Without rejoining, wait 1 second and, as the player, open the tab list. 4. Cleanup: set `features.nameprefix.enabled: false` and `features.nameprefix.prefix-format` back to its shipped default, save, run `ul reload UltiEssentials` from the console, then run `team list` and `team remove` on each `up_` team it shows. | Step 1: the player sees the sidebar; `team list` shows exactly one `up_` team, whose only member is the player; the player's own tab-list name has NO `[UAT]` prefix, because the sidebar is a private scoreboard with no teams. If there is no sidebar, no such team, or the prefix already shows, step 3 would prove nothing: record the row `blocked`, never `pass`. Step 2: the sidebar disappears; chat shows `Scoreboard disabled` (red). Step 3: the player's own tab-list name now shows the gray `[UAT] ` prefix — that team exists only on the server's main scoreboard, so this proves `ScoreboardService#disableScoreboard` returned the player to the main scoreboard rather than to an empty one. Step 4: after the cleanup reload, listing the `up_` team shows no members, and `team list` then shows no `up_` team | java-client | |
| ultiessentials.scoreboard.disable.neg-already | Same as `ultiessentials.scoreboard.disable`, run again while already off | Run `/scoreboard off` a second time | Chat shows `The scoreboard is already disabled` (yellow) rather than the disable-success line | server | |
| ultiessentials.scoreboard.auto-enable-on-join | `features.scoreboard.enabled: true`, `features.scoreboard.auto-enable: true` (both shipped defaults); a player who has never toggled their own scoreboard this session | The player joins the server and waits 1 second | The sidebar scoreboard appears automatically, without the player running any command | server | |
| ultiessentials.scoreboard.disable-on-quit | `features.scoreboard.enabled: true`; `features.scoreboard.auto-enable: false` (so nothing re-adds the player to `enabledPlayers` between quit and rejoin, which would mask the effect this row tests); the player's scoreboard is currently enabled (run `ultiessentials.scoreboard.enable` first) | The player quits, then immediately rejoins and runs `/scoreboard on` | The rejoin's `/scoreboard on` shows the ENABLE-success line (`ultiessentials.scoreboard.enable`'s own line), not the "already enabled" line (`ultiessentials.scoreboard.enable.neg-already`) -- proving `ScoreboardService#enabledPlayers` no longer contained the player's UUID after the quit, since nothing else could have removed it in between | server | |
| ultiessentials.commandalias.rewrite | `language: en`; `features.commandalias.enabled: true` (shipped default); `commandalias.aliases` at its shipped default (includes `gmc: "gamemode creative"`) | As a player NOT holding `ultiessentials.gamemode.self` or `/gamemode` permission, type `/gmc` in chat | The command actually dispatched is `/gamemode creative`, not UltiEssentials' own `/gmc` shortcut — Bukkit's vanilla `/gamemode` permission gate applies, NOT `ultiessentials.gamemode.self`, proving the rewrite happened before permission checking, not after | server | |
| ultiessentials.commandalias.rewrite.neg-disabled | Same as `ultiessentials.commandalias.rewrite`, except `features.commandalias.enabled: false` | Type `/gmc` in chat | UltiEssentials' own `/gmc` shortcut command handles it directly (its own `ultiessentials.gamemode.self` permission gate applies), because no rewrite occurred | server | |
| ultiessentials.cleanup.on-quit | The player has an active `/back` location AND active `/hide` vanish state (run `ultiessentials.back.track` and `ultiessentials.hide.toggle` first) | The player quits | `BackCommand.LAST_LOCATIONS` and `HideCommand.HIDDEN_PLAYERS` both no longer contain the player's UUID immediately after the quit event — confirmed by the subsequent `/back` refusal (`ultiessentials.back.cleanup-on-quit`) and by the player rejoining visible to everyone again with no `/hide` toggle run | server | |

## Player Inspection

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.invsee.view | `language: en`; `features.invsee.enabled: true` (shipped default); an online target other than the sender | Run `/invsee <target>` | The sender's inventory screen opens showing the TARGET's live main inventory (36 slots); chat shows a translated "Viewing `<target>`'s inventory" line; an item moved in this view is reflected in the target's real inventory (live, editable) | server | |
| ultiessentials.invsee.view.neg-offline | `language: en`; `features.invsee.enabled: true`; no player named `does-not-exist` online | Run `/invsee does-not-exist` | Chat shows the translated `Player not found or offline` line; no inventory opens | server | |
| ultiessentials.endersee.view | `language: en`; `features.invsee.enabled: true`; an online target | Run `/endersee <target>` | The sender's inventory screen opens showing the target's live 27-slot ender chest; chat shows a translated "Viewing `<target>`'s ender chest" line | server | |
| ultiessentials.armorsee.view | `language: en`; `features.invsee.enabled: true`; an online target wearing distinguishable armor pieces, other than the sender | Run `/armorsee <target>` | A temporary 9-slot inventory opens showing the target's helmet/chestplate/leggings/boots (in that display order) in slots 0-3 and their off-hand item in slot 5; chat shows a translated "Viewing `<target>`'s equipment" line | server | |
| ultiessentials.armorsee.view.neg-self | `language: en`; `features.invsee.enabled: true` | Run `/armorsee <own-username>` | UNLIKE `ultiessentials.invsee.view.neg-self`'s equivalent, `ArmorseeCommand#armorsee` has no self-target check at all -- the temporary inventory opens showing the sender's OWN armor/off-hand, and the translated "Viewing `<own-username>`'s equipment" line still appears | server | |

## Moderation

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.ban.ban | `language: en`; `features.ban.enabled: true` and `features.ban.broadcast-ban: true` (both shipped defaults); a target player, online or previously-seen offline, not currently banned | Run `/ban <target>` | If online, the target is kicked immediately with the kick screen `You have been banned` / blank / `Reason: No reason` / `Banned by: <sender>` / `Duration: Permanent` / blank / `If you think this is a mistake, contact the server staff`; the server broadcasts `[Ban] <target> was permanently banned by <sender>` then `Reason: No reason` | server | |
| ultiessentials.ban.ban.neg-already-banned | `language: en`; `features.ban.enabled: true`; the target is already actively banned (run `ultiessentials.ban.ban` first) | Run `/ban <same-target>` again | Chat shows `This player is already banned`; no duplicate ban record or duplicate broadcast occurs | server | |
| ultiessentials.ban.ban.neg-broadcast-off | `language: en`; `features.ban.enabled: true` (shipped default); `features.ban.broadcast-ban: false` (NOT the shipped default — set it in `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`, then run `ul reload UltiEssentials` from the console); the issuer is an online player holding `ultiessentials.ban`; a SECOND online player, neither the issuer nor the target, is watching chat; a target not currently banned | The issuer runs `/ban <target> griefing`. Cleanup: set `features.ban.broadcast-ban: true`, run `ul reload UltiEssentials`, then `/unban <target>` | The target is banned as in `ultiessentials.ban.ban` (kicked if online; `/banlist` lists the target). The ISSUER's chat shows the ban notice naming the target and the reason line showing `griefing`; the SECOND player's chat shows neither line. In the cleanup, the second player's chat DOES show the unban notice (`features.ban.broadcast-unban` is still at its default) — the control that their chat is being read; if it does not, record the row `blocked`, because "shows neither line" would then prove nothing. If the issuer sees neither ban line, record `failed`: with the switch off the notice is the issuer's only confirmation, and a ban that succeeds in silence is what this row guards against (UltiKits/UltiEssentials#27) | server | |
| ultiessentials.ban.ban-with-reason | `language: en`; `features.ban.enabled: true`; `features.ban.broadcast-ban: true` (shipped default); a target not currently banned | Run `/ban <target> griefing` | The target is kicked (if online) with `griefing` shown as the reason in the kick screen; the broadcast names both the target and `griefing` | server | |
| ultiessentials.ban.tempban | `language: en`; `features.ban.enabled: true`; `features.ban.broadcast-ban: true` (shipped default); a target not currently banned | Run `/tempban <target> 1h` | The target is kicked (if online) with a "remaining time" section in the kick screen reflecting approximately 1 hour; the broadcast names the target, the fixed default reason, and the formatted duration | server | |
| ultiessentials.ban.tempban.neg-bad-duration | `language: en`; `features.ban.enabled: true` | Run `/tempban <target> notaduration` | Chat shows `Invalid duration format` followed by `Examples: 1d, 2h, 30m, 1w, 1d12h30m`; no ban is created | server | |
| ultiessentials.ban.tempban.neg-broadcast-off | `language: en`; `features.ban.enabled: true` (shipped default); `features.ban.broadcast-ban: false` (NOT the shipped default — set it in `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`, then run `ul reload UltiEssentials` from the console); the issuer is an online player holding `ultiessentials.ban.temp`; a SECOND online player, neither the issuer nor the target, is watching chat; a target not currently banned | The issuer runs `/tempban <target> 1h griefing`. Cleanup: set `features.ban.broadcast-ban: true`, run `ul reload UltiEssentials`, then `/unban <target>` | The target is banned as in `ultiessentials.ban.tempban` (kicked if online; `/banlist` lists the target with about 1 hour remaining). The ISSUER's chat shows the temporary-ban notice naming the target and the formatted duration, and the reason line showing `griefing`; the SECOND player's chat shows neither line. In the cleanup, the second player's chat DOES show the unban notice — the control that their chat is being read; if it does not, record the row `blocked`. If the issuer sees neither ban line, record `failed` (UltiKits/UltiEssentials#27) | server | |
| ultiessentials.ban.tempban-with-reason | `language: en`; `features.ban.enabled: true`; `features.ban.broadcast-ban: true` (shipped default); a target not currently banned | Run `/tempban <target> 1h griefing` | Same as `ultiessentials.ban.tempban`, with `griefing` shown as the reason | server | |
| ultiessentials.ban.unban | `language: en`; `features.ban.enabled: true`; `features.ban.broadcast-unban: true` (shipped default); the target is banned ONLY through this plugin's own record (no separate vanilla `/ban`), run `ultiessentials.ban.ban` first | Run `/unban <target>` | Chat shows `Unbanned <target>`; the server broadcasts `[Unban] <target> has been unbanned`; the target can now rejoin; `/banlist` no longer lists the target, and running `/unban` on the same name again now reports `This player is not banned: <target>` instead of a second success and broadcast | server | |
| ultiessentials.ban.unban.neg-not-banned | `language: en`; `features.ban.enabled: true`; the target has never been banned by this plugin nor by the server's vanilla ban list | Run `/unban <target>` | Chat shows `This player is not banned: <target>` | server | |
| ultiessentials.ban.unban.neg-also-server-banned | `language: en`; `features.ban.enabled: true`; the target is banned by BOTH this plugin's own record AND a separate vanilla `/ban` (run `ultiessentials.ban.ban` on the target, then separately run vanilla `/ban <target>` as an OP) | Run `/unban <target>` | Chat shows a DIFFERENT, TRANSLATED line explicitly stating this plugin's ban was removed but the server's own ban list still bans the player -- `Removed this plugin's ban on <target>, but the server's ban list still bans this player` (`UnbanCommand.java:61-62`, both lines present in `lang/en.json`, unlike the plain-success case at `:64-65`); the broadcast for a plain successful unban does NOT fire; the target still cannot rejoin (vanilla ban still active); `/banlist` no longer lists the target, and lifting the server's own ban alone is then enough to let them rejoin — this module's own record really was deactivated, which is what the message claims. Use whatever route your server offers for the server's own ban list EXCEPT the bare `pardon` label: this module registers `pardon` as an alias of `/unban`, and which registration that label reaches is not established (UltiKits/UltiEssentials#48), so a tester using it may be answered by this module and record a failure against correct behaviour | server | |
| ultiessentials.ban.unban.neg-broadcast-off | `language: en`; `features.ban.enabled: true` (shipped default); `features.ban.broadcast-unban: false` (NOT the shipped default — set it in `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`, then run `ul reload UltiEssentials` from the console); the issuer is an online player holding `ultiessentials.unban`; a SECOND online player, neither the issuer nor the target, is watching chat; the target is banned ONLY through this plugin's own record (run `ultiessentials.ban.ban` first, with the second player already online) | The issuer runs `/unban <target>`. Cleanup: set `features.ban.broadcast-unban: true` and run `ul reload UltiEssentials` | The second player's chat showed the ban notice when `ultiessentials.ban.ban` ran — the control that their chat is being read; if it did not, record the row `blocked`. The ISSUER's chat shows the same plain-success unban line as `ultiessentials.ban.unban`; the SECOND player's chat shows no unban notice; `/banlist` no longer lists the target — the unban happened and only its announcement was withheld (UltiKits/UltiEssentials#27) | server | |
| ultiessentials.banlist.list | `language: en`; `features.ban.enabled: true`; at least one active ban exists (run `ultiessentials.ban.ban` first) | Run `/banlist` | Chat shows the TRANSLATED header `=== Ban List === (1/1)` (`BanListCommand.java:59-60` -- every message in this class is present in `lang/en.json`, unlike almost every other moderation command in this module), then one entry per active ban naming the player, permanent-or-remaining-time, reason, and operator (all translated), then a translated total-count footer reading `Total: 1 active ban` (`:82-83`) | server | |
| ultiessentials.banlist.list.neg-empty | `language: en`; `features.ban.enabled: true`; zero active bans | Run `/banlist` | Chat shows only the translated `No active bans` line (`BanListCommand.java:49` -- translated) -- no header, no entries | server | |
| ultiessentials.banlist.list-page | `language: en`; `features.ban.enabled: true`; more than 10 active bans exist (create 11+ temp-bans against distinct fictitious names via `/tempban`) | Run `/banlist 2` | Page 2 shows entries 11 onward (up to 20), NOT a repeat of page 1's entries | server | |
| ultiessentials.ban.login-check | `features.ban.enabled: true`; the connecting player's UUID has an active, unexpired ban (run `ultiessentials.ban.ban` on that player first, while they are offline) | The banned player attempts to join | The connection is refused at the login stage (`KICK_BANNED`) with the same English kick screen as `ultiessentials.ban.ban`; the player never reaches the world | server | |
| ultiessentials.whitelist.add | `language: en`; `features.whitelist.enabled: true` (shipped default); a player name `Notch` not currently whitelisted | Run `/wl add Notch` | Chat shows a translated "Added Notch to whitelist" line; `/wl list` (see below) now includes `Notch` | server | |
| ultiessentials.whitelist.add.neg-too-long | `language: en`; `features.whitelist.enabled: true` | Run `/wl add ThisNameIsWayTooLongForMinecraft` (17+ characters) | Chat shows the translated `Player name is too long, maximum 16 characters` line; NO `IllegalArgumentException` is thrown and the server does not error — this row is the regression guard for the crash `Bukkit.getOfflinePlayer` used to throw, fixed per UltiKits/UltiEssentials#17 | server | |
| ultiessentials.whitelist.remove | `language: en`; `features.whitelist.enabled: true`; `Notch` is currently whitelisted (run `ultiessentials.whitelist.add` first) | Run `/wl remove Notch` | Chat shows a translated "Removed Notch from whitelist" line; `/wl list` no longer includes `Notch` | server | |
| ultiessentials.whitelist.remove.neg-overlong-legacy-entry | `language: en`; `features.whitelist.enabled: true`; a whitelist entry exists whose stored name is over 16 characters (simulate by hand-editing `whitelist.json` to add such an entry, then restart) | Run `/wl remove <the-overlong-name>` | The removal SUCCEEDS — `WhitelistCommand#remove` matches against the CURRENTLY whitelisted set by string comparison rather than resolving through `Bukkit.getOfflinePlayer`, so an over-length legacy entry can still be removed even though it could never have been re-added via `/wl add` | server | |
| ultiessentials.whitelist.list | `language: en`; `features.whitelist.enabled: true`; `Notch` is whitelisted (run `ultiessentials.whitelist.add` first) | Run `/wl list` | Chat shows a translated `Whitelist (1): Notch`-shaped line (count and comma-joined names) | server | |
| ultiessentials.whitelist.list.neg-empty | `language: en`; `features.whitelist.enabled: true`; zero whitelisted players | Run `/wl list` | Chat shows the translated `Whitelist is empty` line | server | |
| ultiessentials.whitelist.enable | `language: en`; `features.whitelist.enabled: true`; server whitelist enforcement currently off | Run `/wl on` | `Bukkit.hasWhitelist()` becomes true; chat shows the translated `Whitelist enabled` line; a non-whitelisted player can no longer join | server | |
| ultiessentials.whitelist.disable | `language: en`; `features.whitelist.enabled: true`; server whitelist enforcement currently on (run `ultiessentials.whitelist.enable` first) | Run `/wl off` | `Bukkit.hasWhitelist()` becomes false; chat shows the translated `Whitelist disabled` line | server | |
| ultiessentials.whitelist.status | `language: en`; `features.whitelist.enabled: true`; whitelist currently on with `Notch` the only entry (run `ultiessentials.whitelist.enable` and `ultiessentials.whitelist.add` first) | Run `/wl status` | Chat shows a translated `Whitelist status: Enabled, count: 1`-shaped line | server | |

## Container Locking

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.lock.lock | `language: en`; `features.chestlock.enabled: true` (shipped default); the player looking directly at an unlocked chest within 5 blocks | Run `/lock` | Chat shows `Container locked` (green); the container can no longer be opened by any other player lacking `ultiessentials.lock.admin` | server | |
| ultiessentials.lock.lock.neg-not-lockable | `language: en`; `features.chestlock.enabled: true`; the player looking at a non-lockable block (e.g. plain stone) within 5 blocks | Run `/lock` | Chat shows `This block cannot be locked` | server | |
| ultiessentials.lock.lock.neg-already-locked | `language: en`; `features.chestlock.enabled: true`; a chest already locked by a DIFFERENT player (run `ultiessentials.lock.lock` as a different player first) | Run `/lock` as the second player | Chat shows `This container is locked by another player`; ownership of the lock is unchanged | server | |
| ultiessentials.lock.lock.neg-already-locked-by-you | Same as `ultiessentials.lock.lock`, run a second time by the SAME player who already locked it | Run `/lock` again as the owner | Chat shows `You have already locked this container` (yellow), distinct from the other-owner case | server | |
| ultiessentials.lock.protect-whole-container | `features.chestlock.enabled: true`; a double chest locked by one player (run `ultiessentials.lock.lock` on it first); a second, non-operator player who does not hold `ultiessentials.lock.admin` | The second player right-clicks the LEFT half, then the RIGHT half | Both clicks are refused with the container-locked message naming the owner, and neither opens the chest — the two halves are one inventory, so a lock on it is enforced from either side | server | |
| ultiessentials.lock.unlock | `language: en`; a chest locked by the acting player (run `ultiessentials.lock.lock` first) | Run `/unlock` | Chat shows `Container unlocked` (green); the container can now be opened by anyone again | server | |
| ultiessentials.lock.unlock.neg-not-locked | `language: en`; the looked-at container is not locked | Run `/unlock` | Chat shows `This container is not locked` (red) | server | |
| ultiessentials.lock.unlock.neg-not-owner | `language: en`; a chest locked by a DIFFERENT player; the acting player does NOT hold `ultiessentials.lock.admin` | Run `/unlock` | Chat shows `You are not the owner of this container`; the lock is unchanged | server | |
| ultiessentials.lock.unlock.neg-disabled-still-works | `language: en`; `features.chestlock.enabled: false` (NOT the shipped default); a container that was locked while the feature was still enabled | Run `/unlock` | Unlocking still succeeds — `ChestLockService#unlockBlock` has no `chestlock.enabled` check at all, unlike `#lockBlock` (see FEATURES.md `## Container Locking` section note) | server | |
| ultiessentials.lock.info | `language: en`; a chest locked by some player (run `ultiessentials.lock.lock` first) | Run `/unlock info` | Chat shows the gold header `=== Container Lock Info ===`, then `Owner: <name>` and `Location: <world> (<x>, <y>, <z>)` | server | |
| ultiessentials.lock.info.neg-not-locked | `language: en`; the looked-at container is not locked | Run `/unlock info` | Chat shows `This container is not locked` (gray), distinct from the header shown in the positive case | server | |
| ultiessentials.chestlock.protect-interact | `features.chestlock.enabled: true`; a chest locked by a different player; the interacting player does NOT hold `ultiessentials.lock.admin`, and `chestlock.admin-bypass` is irrelevant since they hold no admin permission at all | Right-click the locked chest | The inventory does NOT open; the event is cancelled; the interacting player receives `This container is locked by <owner>` | server | |
| ultiessentials.chestlock.protect-interact.neg-admin-bypass | Same as `ultiessentials.chestlock.protect-interact`, except the interacting player holds `ultiessentials.lock.admin` AND `features.chestlock.admin-bypass: true` (shipped default) | Right-click the same locked chest | The inventory DOES open normally | server | |
| ultiessentials.chestlock.protect-break | `features.chestlock.enabled: true`; a chest locked by a different player; the breaking player holds neither ownership nor `ultiessentials.lock.admin` | Attempt to break the locked chest | The break is cancelled; the breaking player receives `This container is locked by <owner> and cannot be broken` — a different line from the interact refusal, built in one place (`ChestLockListener#breakRefusal`) and sent by BOTH `onPlayerInteract`, which is what an ordinary mining attempt reaches, and `onBlockBreak`, which is what an API-driven break reaches; the block remains intact and still locked | server | |
| ultiessentials.chestlock.protect-break.neg-owner | Same as `ultiessentials.chestlock.protect-break`, except the breaking player is the lock's owner | Break the chest | The break succeeds; the lock record for that location is removed (confirm via `/unlock info` on a re-placed chest at the same coordinates reporting "not locked"), and the server log carries no lock-removal error for that location | server | |
| ultiessentials.chestlock.protect-explode-entity | `features.chestlock.enabled: true`; a locked chest adjacent to a TNT block that will explode; a SECOND, UNLOCKED control block of the same breakable material placed at an equal distance from the TNT, in a direction with no other interference, so the explosion's own randomness can be judged against a like-for-like comparison rather than assuming every block in range is destroyed | Detonate the TNT | The unlocked control block IS destroyed by the explosion, but the locked chest at the equal distance survives intact and remains locked -- `ChestLockListener#onEntityExplode` only removes locked locations from `event.blockList()` and makes no guarantee about any other block in range | server | |
| ultiessentials.chestlock.protect-explode-block | `features.chestlock.enabled: true`; a locked chest positioned to be destroyed by a bed/respawn-anchor mismatched-dimension explosion; a SECOND, UNLOCKED control chest at an equal distance from the charge, in a direction with no other interference — required for the same reason `ultiessentials.chestlock.protect-explode-entity` requires one, and stated here because this row previously did not: without it, a blast that destroyed nothing at that distance would let the locked chest's survival pass vacuously | Trigger the block explosion | The unlocked control chest IS destroyed, and the locked chest at the equal distance survives intact and remains locked. As with the entity explosion, `ChestLockListener#onBlockExplode` only removes locked locations from `event.blockList()` and makes no guarantee about any other block in range | server | |
| ultiessentials.chestlock.protect-piston-extend | **This row does not verify the protection guard, and cannot: vanilla forecloses the path.** It verifies the platform rule the guard's unreachability rests on, so that the day a Minecraft release changes that rule, this row fails and tells us the guard needs real-machine coverage after all. An earlier revision asserted the opposite — that `Material.FURNACE` is "a plain, ordinary block with no special piston reaction — pistons push and pull it exactly like stone or dirt" — which is false, and false for exactly the reason the same sentence gave for excluding a chest: a furnace is a standard tile-entity container. Measured in `paper-1.21.11-132.jar` (`versions/1.21.11/paper-1.21.11.jar`): `PistonBaseBlock.isPushable` ends `return !state.hasBlockEntity()` (bytecode offsets 250-262), `BlockStateBase.hasBlockEntity()` is `getBlock() instanceof EntityBlock` (offsets 0-7), and every one of the eleven block classes behind `ChestLockService.LOCKABLE_BLOCKS` is an `EntityBlock` (`ChestBlock`/`TrappedChestBlock` via `AbstractChestBlock`, `FurnaceBlock`/`BlastFurnaceBlock`/`SmokerBlock` via `AbstractFurnaceBlock`, `DropperBlock` via `DispenserBlock`, and `BarrelBlock`, `ShulkerBoxBlock`, `HopperBlock`, `DispenserBlock`, `BrewingStandBlock` directly — all extending `BaseEntityBlock implements EntityBlock`; controls `StairBlock` and `SandBlock` are not, so the check discriminates). For extension, `PistonBaseBlock.checkIfExtend` sends the extend block event only when a `PistonStructureResolver`'s `resolve()` returns true (offsets 43-70), and `resolve()` first tests the block in front with `isPushable` and returns false when that fails and the block is not one that pushing destroys (offsets 30-88; the furnace keeps the default push reaction, `NORMAL`). A powered piston facing a furnace therefore never extends, and `BlockPistonExtendEvent`, which is built only in `moveBlocks` (offset 234), is never fired for it. **No substitute block exists** — no member of `LOCKABLE_BLOCKS` can ever appear in `getBlocks()`. The guard's own behaviour is covered by unit tests instead, named in Expected below. The harness cannot read an event's `getBlocks()`, so this row reads the world instead. Fixture, built from the console in open air over a solid floor, with nothing else within two blocks: `features.chestlock.enabled: true`; three unpowered `minecraft:piston[facing=east]` at (`Xp`, `Y`, `Z1`), (`Xp`, `Y`, `Z1`+3) and (`Xp`, `Y`, `Z1`+6), with air at `Xp`-1 and at `Xp`+2 in each of the three rows; in front of them at `Xp`+1, in that order, `minecraft:stone` (the positive control that proves the pistons fire at all), a `minecraft:furnace` to be locked, and a second, UNLOCKED `minecraft:furnace`; each furnace given one cobblestone in its input slot (`item replace block <furnace> container.0 with minecraft:cobblestone 1`); a player looking directly at the first furnace from within 5 blocks has run `/lock` (`ultiessentials.lock.lock`), `/unlock info` on it names that player as the owner (`ultiessentials.lock.info`), and `/unlock info` on the second furnace says it is not locked (`ultiessentials.lock.info.neg-not-locked`) | 1. In each of the three rows, run `setblock <Xp-1> <Y> <Z> minecraft:redstone_block` from the console. 2. Wait at least one second. 3. From the console, read back each row: `execute if block <Xp-1> <Y> <Z> minecraft:redstone_block`; the piston, with `execute if block <Xp> <Y> <Z> minecraft:piston[extended=true]` and `minecraft:piston[extended=false]`; what stands at `Xp`+1 and at `Xp`+2, with `execute if block` against `minecraft:stone`, `minecraft:piston_head`, `minecraft:furnace` and `minecraft:air`; then `data get block <Xp+1> <Y> <Z> Items` for each furnace row. 4. Cleanup: the owner runs `/unlock` on the locked furnace (`ultiessentials.lock.unlock`), then every fixture block is set back to air | Record `pass` only when every read-back matches: all three redstone blocks are in place; the stone control IS pushed — its piston reads `extended=true`, a `minecraft:piston_head` stands at `Xp`+1 and the stone at `Xp`+2 (without this the row passes vacuously); and NEITHER furnace moves, locked or unlocked — each furnace's piston reads `extended=false`, the furnace still stands at `Xp`+1, `Xp`+2 is still air, and `data get block` still lists the one cobblestone the fixture gave it. That is the platform refusing: `resolve()` rejected the furnace, so no `BlockPistonExtendEvent` was fired for it and `ChestLockListener#onPistonExtend` was never reached. Reading "the platform refused" from the UNLOCKED furnace's retracted piston rests on nothing cancelling a piston event for an unlocked container; for this module that is `ChestLockListenerTest$PistonExtendTests`' `Should not cancel when no locked blocks`. Record `fail` if either furnace has moved — it no longer stands at `Xp`+1, a furnace stands at `Xp`+2, or its piston reads `extended=true` — which means the platform rule changed and the guard now needs a real-machine row of its own; in that case also record whether the LOCKED furnace stayed put, which would be the guard's first real-machine evidence. Record `blocked`, never `pass`, on anything else: a redstone block missing, a control that did not move, a lock or a non-lock not confirmed in the fixture, an `Items` read-back missing or different. **Do NOT read a `pass` here as evidence the protection listener works** — that claim rests on `ChestLockListenerTest$PistonExtendTests` (`Should cancel when pushing locked block`, `Should not cancel when no locked blocks`, `Should skip when feature is disabled`), and never on this row | server | |
| ultiessentials.chestlock.protect-piston-retract | **This row does not verify the protection guard, and cannot**, for the platform reason measured in `ultiessentials.chestlock.protect-piston-extend` above, which gates retraction too, through a different call: a sticky piston's `PistonBaseBlock.triggerEvent` tests the block two ahead of it with `isPushable` directly (offset 513) and, when that fails, only removes its own head (offset 602) without calling `moveBlocks`, the only place a `BlockPistonRetractEvent` carrying moved blocks is built. A tile-entity container is therefore no more pullable than pushable, and never appears in a retract event's `getBlocks()`. An earlier revision said retraction was gated "through the same `PistonStructureResolver`"; `resolve()` is reached on retraction only for a block that has already passed `isPushable`. The harness cannot read an event's `getBlocks()`, so this row reads the world instead. Fixture, built from the console in open air over a solid floor, with nothing else within two blocks: `features.chestlock.enabled: true`; three unpowered `minecraft:sticky_piston[facing=east]` at (`Xp`, `Y`, `Z1`), (`Xp`, `Y`, `Z1`+3) and (`Xp`, `Y`, `Z1`+6), with air at `Xp`-1 and at `Xp`+1 in each row (the head needs that gap to extend: a sticky piston directly against a furnace cannot extend at all, so there would be nothing to retract); at `Xp`+2, in that order, `minecraft:stone` (the positive control), a `minecraft:furnace` to be locked, and a second, UNLOCKED `minecraft:furnace`; each furnace given one cobblestone in its input slot; the lock and the non-lock set up and confirmed exactly as in `ultiessentials.chestlock.protect-piston-extend` | 1. In each row, run `setblock <Xp-1> <Y> <Z> minecraft:redstone_block`, wait at least one second, and read back `execute if block <Xp> <Y> <Z> minecraft:sticky_piston[extended=true]`, `execute if block <Xp+1> <Y> <Z> minecraft:piston_head`, and the target at `Xp`+2 (`minecraft:stone` or `minecraft:furnace`). 2. In each row, run `setblock <Xp-1> <Y> <Z> minecraft:air` and wait at least one second. 3. Read back each row: `execute if block <Xp> <Y> <Z> minecraft:sticky_piston[extended=false]`, and what stands at `Xp`+1 and at `Xp`+2, with `execute if block` against `minecraft:stone`, `minecraft:furnace` and `minecraft:air`; then `data get block <Xp+2> <Y> <Z> Items` for each furnace row. 4. Cleanup: the owner runs `/unlock` on the locked furnace (`ultiessentials.lock.unlock`), then every fixture block is set back to air | After step 1, all three sticky pistons read `extended=true` with a `minecraft:piston_head` at `Xp`+1 and their targets untouched at `Xp`+2; if any did not extend, the fixture never armed and the row is `blocked`. Record `pass` only when, after step 3, every read-back matches: the stone control IS pulled back — stone at `Xp`+1, air at `Xp`+2 (without this the row passes vacuously); and NEITHER furnace is pulled, locked or unlocked — each sticky piston reads `extended=false`, `Xp`+1 is air (the head retracted and brought nothing), the furnace still stands at `Xp`+2, and `data get block` still lists the one cobblestone the fixture gave it. That is the platform refusing: `isPushable` failed for the furnace, so no retract event listed it and `ChestLockListener#onPistonRetract` was never reached. As in the extend row, reading "the platform refused" from the UNLOCKED furnace rests on nothing cancelling a piston event for an unlocked container (`ChestLockListenerTest$PistonRetractTests`' `Should not cancel when no locked blocks`). Record `fail` if either furnace is pulled — a furnace stands at `Xp`+1, or `Xp`+2 is no longer a furnace — which means the platform rule changed and the guard now needs a real-machine row; in that case also record whether the LOCKED furnace stayed put. Record `blocked`, never `pass`, on anything else. **Do NOT read a `pass` here as evidence the protection listener works** — that claim rests on `ChestLockListenerTest$PistonRetractTests` (`Should cancel when pulling locked block`, `Should not cancel when no locked blocks`, `Should skip when feature is disabled`), and never on this row | server | |
| ultiessentials.chestlock.protect-hopper | `features.chestlock.enabled: true`; a hopper feeding directly out of a locked chest; a SECOND, UNLOCKED control chest over its own hopper in the same fixture, also holding items — required because "the hopper took nothing" is equally true of a hopper that was never able to take anything (wrong facing, unpowered, no items), so without the control this row passes vacuously | Place items in both chests and wait several hopper cycles (at least 10 seconds) | The control hopper DOES drain the unlocked chest, and over the same period the locked chest's contents are unchanged — no item leaves it, and the total item count across chest and hopper is conserved | server | |
| ultiessentials.chestlock.protect-break.whole-container | `language: en`; `features.chestlock.enabled: true`; a **partly-recorded double chest**: run `ultiessentials.lock.lock` as player A on a SINGLE chest, then, as A, place a second chest directly against it so the two become one double chest. Only the first block holds a lock record; the newly placed half holds none. Confirm the fixture before testing: A opens the chest and sees a 54-slot double inventory, and `/unlock info` aimed at the FIRST block reports A as owner. If the inventory is 27 slots the two blocks did not pair (they must face the same way and share a side): record the row `blocked`, never `pass`; player A has put a stack of a recognisable item (e.g. 32 diamonds) into the chest; a second player B who is not an operator and holds neither `ultiessentials.lock.admin` nor ownership | As B, attempt to break the SECOND (newly placed, unrecorded) half | The break is refused: the block is still standing afterwards, NO item entity appears on the ground next to it, and B receives `This container is locked by A and cannot be broken` (built in one place, `ChestLockListener#breakRefusal`, and sent by BOTH `onPlayerInteract` and `onBlockBreak`), which is the same message the already-recorded half gives. If the block breaks, its share of the contents drops on the floor for B to collect — that is the defect this row exists to catch, and the row is `fail`, not `blocked` | server | |
| ultiessentials.chestlock.protect-break.whole-container.neg-owner | `language: en`; `features.chestlock.enabled: true`; a **partly-recorded double chest**: run `ultiessentials.lock.lock` as player A on a SINGLE chest, then, as A, place a second chest directly against it so the two become one double chest. Only the first block holds a lock record; the newly placed half holds none. Confirm the fixture before testing: A opens the chest and sees a 54-slot double inventory, and `/unlock info` aimed at the FIRST block reports A as owner. If the inventory is 27 slots the two blocks did not pair (they must face the same way and share a side): record the row `blocked`, never `pass` | As A, break the SECOND (unrecorded) half | The break succeeds — widening the check must not lock an owner out of their own container. The FIRST half is still standing and still locked: `/unlock info` aimed at it still reports A | server | |
| ultiessentials.chestlock.protect-explode-entity.whole-container | `language: en`; `features.chestlock.enabled: true`; a **partly-recorded double chest**: run `ultiessentials.lock.lock` as player A on a SINGLE chest, then, as A, place a second chest directly against it so the two become one double chest. Only the first block holds a lock record; the newly placed half holds none. Confirm the fixture before testing: A opens the chest and sees a 54-slot double inventory, and `/unlock info` aimed at the FIRST block reports A as owner. If the inventory is 27 slots the two blocks did not pair (they must face the same way and share a side): record the row `blocked`, never `pass`; a TNT block placed to destroy the SECOND (unrecorded) half, plus a SECOND, UNLOCKED control chest of the same material at an equal distance from the TNT in an uninterfered direction, so the explosion's own randomness is judged against a like-for-like comparison | Detonate the TNT | The unlocked control chest IS destroyed, while the unrecorded half of the locked double chest survives intact and still pairs with its other half. A TNT charge must not do what a pickaxe may not | server | |
| ultiessentials.chestlock.protect-hopper.double-chest | `language: en`; `features.chestlock.enabled: true`; a double chest BOTH halves of which are locked by player A (run `ultiessentials.lock.lock` once on either half of an already-paired double chest, which records both); a hopper placed directly beneath either half, feeding out of it; items in the chest; a SECOND, UNLOCKED control chest over its own hopper in the same fixture, also holding items, for the same anti-vacuous reason `ultiessentials.chestlock.protect-hopper` above states | Put items in the locked double chest and wait several hopper cycles (at least 10 seconds) | The control hopper DOES drain the unlocked chest over the same period, while the locked double chest loses NO item and its contents are unchanged. A double chest's inventory holder is a `DoubleChest` rather than a `Container`, so a holder type-test against `Container` skipped double chests entirely and left even a fully-locked one drainable — this row is the one that distinguishes a single from a double chest, and `ultiessentials.chestlock.protect-hopper` above cannot catch it | server | |
| ultiessentials.lock.lock.neg-other-half-owned | `language: en`; `features.chestlock.enabled: true`; player A has run `ultiessentials.lock.lock` on a SINGLE chest; a second player B, not an operator, holding `ultiessentials.lock` but neither ownership nor `ultiessentials.lock.admin` | 1. As B, place a chest directly against A's locked chest so the two pair into one double chest. 2. As B, look at the half B just placed and run `/lock`. 3. As B, right-click that same half. | Step 2: chat shows `This container is locked by another player`, NOT `You have already locked this container` and NOT the success line — B may not put a record of their own on a container A holds. Step 3: the inventory does not open. If step 2 reports success, B now co-owns a record on A's container and can unlock it out from under A: the row is `fail` | server | |
| ultiessentials.lock.unlock.from-unrecorded-half | `language: en`; a **partly-recorded double chest**: run `ultiessentials.lock.lock` as player A on a SINGLE chest, then, as A, place a second chest directly against it so the two become one double chest. Only the first block holds a lock record; the newly placed half holds none. Confirm the fixture before testing: A opens the chest and sees a 54-slot double inventory, and `/unlock info` aimed at the FIRST block reports A as owner. If the inventory is 27 slots the two blocks did not pair (they must face the same way and share a side): record the row `blocked`, never `pass` | As A, look at the SECOND (unrecorded) half and run `/unlock` | Chat shows `Container unlocked`, not `This container is not locked` — the question is about the container, not the block aimed at. Afterwards the container opens for any player, and `/unlock info` aimed at EITHER half reports it not locked | server | |
| ultiessentials.lock.info.whole-container | `language: en`; a **partly-recorded double chest**: run `ultiessentials.lock.lock` as player A on a SINGLE chest, then, as A, place a second chest directly against it so the two become one double chest. Only the first block holds a lock record; the newly placed half holds none. Confirm the fixture before testing: A opens the chest and sees a 54-slot double inventory, and `/unlock info` aimed at the FIRST block reports A as owner. If the inventory is 27 slots the two blocks did not pair (they must face the same way and share a side): record the row `blocked`, never `pass` | As A, aim at the SECOND (unrecorded) half and run `/unlock info` | Chat shows the lock-info header `=== Container Lock Info ===` followed by A's name and the coordinates of the FIRST half — the block that actually holds the record. It must NOT show `This container is not locked`, which would contradict the refusal `ultiessentials.lock.protect-whole-container` gives for a right-click on that very block | server | |

## Server Presentation

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.motd.customize | `features.motd.enabled: true` (shipped default); `config/motd.yml` at its shipped default | Ping the server's status from a client's server list (or an equivalent status query tool) | The displayed MOTD is exactly the two configured lines joined by a newline, color-coded per `&6`/`&7`, NOT the platform's own `motd` from `server.properties` | os-input | |
| ultiessentials.motd.customize.neg-max-players-override | Same as `ultiessentials.motd.customize`, except `motd.max-players: 5` (NOT the shipped `-1`) | Ping the server's status | The displayed max-player count reads `5`, overriding the platform's real configured value | os-input | |
| ultiessentials.tabbar.on-join | `language: en`; `features.tab-bar.enabled: true` (shipped default); `config/tabbar.yml` at its shipped default, written in English at module start (`## Config text materializer` below); at least 2 players online | Join the server as a NEW connection (`TabBarListener#onPlayerJoin` calls `updateTabBar` only for the joining player -- an already-online player pressing Tab after someone else joins does NOT refresh their own header/footer) | Under `language: en` the tab list header reads `=== Server Name ===` (gold) and the footer `Online: <online>/<max>` with the current live counts — exactly `tabbar.header` and `tabbar.footer` as the file holds them | java-client | |
| ultiessentials.nameprefix.on-join | `features.nameprefix.enabled: true` (NOT the shipped `false` — flip it on for this row); `features.nameprefix.prefix-format` at its shipped default; `features.scoreboard.enabled: false` (NOT the shipped `true` -- REQUIRED for this row: `ScoreboardService#updateScoreboard` calls `player.setScoreboard(manager.getNewScoreboard())` at the scoreboard feature's own 1-second auto-enable delay, which reassigns the player away from the main scoreboard `NamePrefixService` registers its team on at 0.5 seconds, masking the prefix for that player if left at the shipped default); a Vault-compatible permissions plugin providing a `%vault_prefix%` value (or PlaceholderAPI absent, exercising the raw-token fallback); after changing these config values, restart the server or run `ul reload UltiEssentials` from the console (a reload restarts `NamePrefixService` against them, UltiKits/UltiEssentials#28) | Join the server and wait 0.5 seconds | The player's name, as shown above their head and in the tab list, carries the configured prefix template; a distinct player's prefix does not affect this one (per-player team entry on the shared main scoreboard) | java-client | |
| ultiessentials.nameprefix.on-quit | Same feature-enabled precondition as `ultiessentials.nameprefix.on-join`; the player has an active prefix (run that row first) | The player quits, then a DIFFERENT observer checks the scoreboard team's entry list | The quitting player's name is no longer a member of any name-prefix team (source-level check: `NamePrefixService#playerTeams` no longer maps their UUID after the quit event — read `NamePrefixListener.java:76-78`) | protocol | |
| ultiessentials.deathpunish.on-death | `features.deathpunish.enabled: true` AND `features.deathpunish.exp.enabled: true` (BOTH NOT the shipped default of `false` -- flip both on explicitly; enabling the parent toggle does not change the `exp.enabled` sub-toggle's own default); `features.deathpunish.exp.percent: 20.0` (shipped default once `exp.enabled` is explicitly turned on); the player has AT LEAST 50 total experience points (so `(int)(total * 0.20)` is comfortably positive and, per `PlayerDeathEvent#getDroppedExp()`'s own vanilla-computed value at that experience level, not clamped to zero by `Math.max(0, event.getDroppedExp() - expLoss)`); the player does NOT hold `ultiessentials.deathpunish.bypass`; the player is not in a whitelisted world (`world_creative`, shipped default, is excluded — die elsewhere) | Die as the player | The player's dropped experience orbs are reduced by `(int)(total_experience * 0.20)` (an integer truncation of 20% of their PRE-DEATH total experience, not 20% of the vanilla-computed drop amount alone -- confirm by comparing to a control death with `features.deathpunish.enabled: false`), CLAMPED to never go below zero even if the computed loss exceeds the vanilla drop amount; the player receives the summary line `Death penalty: -<n> EXP` (red prefix, green amount) naming the experience loss | server | |
| ultiessentials.deathpunish.on-death.neg-bypass | Same as `ultiessentials.deathpunish.on-death`, except the player holds `ultiessentials.deathpunish.bypass` | Die as the player | No punishment of any kind applies; the vanilla death behaviour is completely unmodified | server | |
| ultiessentials.deathpunish.on-death.neg-world-whitelist | Same as `ultiessentials.deathpunish.on-death`, except the player dies inside `world_creative` (the shipped `deathpunish.world-whitelist` entry) | Die as the player in `world_creative` | No punishment applies, even though the player holds no bypass permission | server | |

## Scheduled Commands

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.scheduledcommands.run | `features.scheduled-commands.enabled: true` (NOT the shipped `false` — flip it on); `scheduled-commands.commands` at its shipped default (`300:say Server is online!`, `600:broadcast &cReminder: follow server rules!`); restart the server after changing the config, because Steps time the intervals from server start (a `ul reload UltiEssentials` also reschedules this service, UltiKits/UltiEssentials#28, but its intervals then count from the reload) | Wait 300 seconds after server start | The console/chat shows the `say Server is online!` output; at 600 seconds the `&c`-colored broadcast reminder also appears; both then repeat on the same intervals indefinitely | server | |
| ultiessentials.scheduledcommands.run.neg-malformed-entry | Same base preconditions as `ultiessentials.scheduledcommands.run`, except `features.scheduled-commands.commands` includes one additional malformed entry with no colon (e.g. `"broadcast oops"`) | Restart the server and check the startup log | A warning naming the malformed entry is logged (`ScheduledCommandService#startTasks`'s own `log.warn`); the OTHER, well-formed entries still schedule and run normally — one bad entry does not abort the whole list | server | |

## Lifecycle

The rows below exercise UltiKits/UltiEssentials#23's lifecycle migration, the `onReload()` hook
added by UltiKits/UltiEssentials#28 and the `onUnregister()` hook added by
UltiKits/UltiEssentials#43 (see `FEATURES.md`'s `## Lifecycle`). `/ul reload <name>` matches `<name>` case-insensitively against each loaded
module's runtime name (the `name:` key of the module's own `plugin.yml`, `UltiEssentials` here),
calls that module's `reloadSelf()`, which as of UltiTools 6.3.0 re-reads the module's config files
into the running beans and logs the framework's own line `Module '<name>' reloaded.`, then replies
to the sender. This module's main config file on a server is
`plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`. The first row changes
`features.speed.max-speed`, which `SpeedCommand#setSpeed` reads on every call. The second row turns
`features.nameprefix.enabled` on through a reload, which restarts `NamePrefixService` against the
re-read value. The third row turns `features.scoreboard.enabled` off through a reload, which must
return players to the main scoreboard. The fourth row leaves two settings this module removed in
its file and reads the start-up warning they produce, then removes them and reads that a reload
produces none. Name-prefix teams live on the server's main scoreboard, which the vanilla console
command `team list` reads: `NamePrefixService` names each player's team `up_` followed by the first
8 characters of that player's UUID, and keeps the team registered (only its entry is removed) when
the player quits, when name prefixes are turned off, or when the module is unloaded.
The last three rows unload the module instead of reloading it. `/upm uninstall <name>` matches the
same runtime name as `/ul reload`, unloads the module and then deletes its JAR from
`plugins/UltiTools/plugins/` — so all three rows need a copy of that JAR kept elsewhere beforehand, and
all three put it back in their cleanup. The first two observe the three repeating tasks named in
UltiKits/UltiEssentials#43 plus the teleport warmup that shares the defect: a task that is still
running is visible as console output, a sidebar, a team member or a teleport that still happens, and
each row establishes that it WAS running before the uninstall, so a row in which nothing was running
cannot pass. The third observes a vanish: it establishes the player WAS hidden before the uninstall,
then reads whether they are visible to everyone after it.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.lifecycle.reload | `language: en` in `plugins/UltiTools/config.yml` (every quoted line below is translated under it: the three `/speed` lines by this module's `lang/en.json`, the two reload lines by the framework); `UltiEssentials` loaded; `features.speed.enabled: true` and `features.speed.max-speed: 10` (both shipped defaults) in `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`; an online player holding `ultiessentials.speed`; `/ul` runs from the server console (it requires op) | 1. As the player, run `/speed 7`. 2. As the player, run `/speed reset`. 3. Edit `features.speed.max-speed` to `5` in that `essentials.yml` and save the file; from the server console, on the SAME running server (no restart), run `ul reload UltiEssentials`. 4. As the player, run `/speed 7` again. 5. Cleanup: set `features.speed.max-speed` back to `10`, save, run `ul reload UltiEssentials` from the console, and as the player run `/speed reset`. | Step 1: chat shows `Speed set to 7` — this proves 7 is within the starting limit; if it shows `Speed must be between 0-<n>` instead, the starting value is not 10: record the row `blocked`, never `pass`. Step 2: chat shows `Speed reset to default`. Step 3: the console shows `Module 'UltiEssentials' reloaded.` and the console command reply `Module UltiEssentials has been reloaded`, with no reload line from the module itself. Step 4: chat shows `Speed must be between 0-5` and no `Speed set to` line — the edited `features.speed.max-speed` took effect without a restart. On UltiTools 6.2.5 the pre-migration module never re-read its configuration on `/ul reload`, so step 4 would show `Speed set to 7`; on 6.3.0 that pre-migration jar does not load at all, so step 1 already fails | server | |
| ultiessentials.lifecycle.reload-nameprefix | `language: en` in `plugins/UltiTools/config.yml`; `UltiEssentials` loaded; in `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`, BEFORE the server's last restart: `features.nameprefix.enabled: false` (shipped default), `features.nameprefix.prefix-format: "&7[UAT] "` (NOT the shipped default: a literal with no placeholder, so the prefix depends on neither Vault nor PlaceholderAPI), `features.nameprefix.suffix-format: ""` (shipped default) and `features.scoreboard.enabled: false` (NOT the shipped `true` — same reason as `ultiessentials.nameprefix.on-join`: a sidebar moves the player off the main scoreboard the prefix team is on); before that restart, `team list` from the console listed no team whose name starts with `up_` (remove any left from an earlier run with `team remove <name>`); two accounts, A online and B offline; `/ul` and `team` run from the server console | 1. From the console run `team list`, and as A open the tab list. 2. Edit `features.nameprefix.enabled` to `true` in that `essentials.yml` and save; from the console, on the SAME running server (no restart), run `ul reload UltiEssentials`. 3. Wait 2 seconds; from the console run `team list`, then `team list <the up_ team it shows>`; as A open the tab list. 4. Join as B and wait 1 second; from the console run `team list`, then `team list` on each `up_` team; as A open the tab list; read the console output since step 2. 5. Cleanup: set `features.nameprefix.enabled: false`, `features.nameprefix.prefix-format` back to its shipped default and `features.scoreboard.enabled: true`, save, run `ul reload UltiEssentials`, then run `team list` and `team remove` on each `up_` team it shows. | Step 1: `team list` shows no team whose name starts with `up_`, and A's tab-list name has no `[UAT]` prefix; if an `up_` team exists or A carries `[UAT]`, name prefixes were not off at boot: record the row `blocked`, never `pass`. Step 2: the console shows `Module 'UltiEssentials' reloaded.` and no exception stack trace. Step 3: `team list` shows exactly one `up_` team and listing it names A as its only member; A's tab-list name reads `[UAT] ` in gray followed by A's name — the prefix was applied by reload, without a restart. Step 4: `team list` shows exactly two `up_` teams, one naming A and one naming B as its only member; A's tab list shows both A and B with the gray `[UAT] ` prefix; the console shows no `NullPointerException` and no stack trace naming `NamePrefixService` since step 2. Step 5: after the cleanup reload, listing each `up_` team shows no members, and `team list` then shows no `up_` team | java-client | |
| ultiessentials.lifecycle.reload-scoreboard-off | `language: en` in `plugins/UltiTools/config.yml`; `UltiEssentials` loaded; in `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`, BEFORE the server's last restart: `features.nameprefix.enabled: true` (NOT the shipped `false`), `features.nameprefix.prefix-format: "&7[UAT] "` (NOT the shipped default: a literal with no placeholder), `features.nameprefix.suffix-format: ""`, and `features.scoreboard.enabled: true` and `features.scoreboard.auto-enable: true` (both shipped defaults); two accounts A and B, each joined AFTER that restart and online for at least 5 seconds (so each has a name-prefix team and, 1 second after joining, a sidebar); `/ul` and `team` run from the server console | 1. As B, look at the screen and open the tab list; from the console run `team list`, then `team list` on each `up_` team it shows. 2. Edit `features.scoreboard.enabled` to `false` in that `essentials.yml` and save; from the console, on the SAME running server (no restart), run `ul reload UltiEssentials`. 3. Without either account rejoining, wait 2 seconds; as B look at the screen and open the tab list; from the console run `team list` on each `up_` team. 4. Cleanup: set `features.scoreboard.enabled: true`, `features.nameprefix.enabled: false` and `features.nameprefix.prefix-format` back to its shipped default, save, run `ul reload UltiEssentials`, then run `team list` and `team remove` on each `up_` team it shows. | Step 1: B sees the sidebar, titled `Server Info` at the shipped `features.scoreboard.title`, written into the file in English (or the configured title); `team list` shows two `up_` teams, one naming A and one naming B as its only member; B's tab list shows A and B WITHOUT the `[UAT]` prefix, because a sidebar is a private scoreboard with no teams (the same masking `ultiessentials.nameprefix.on-join` avoids). If B has no sidebar, if no `up_` team names A or B, or if B's tab list already shows `[UAT]`, the starting state is wrong and step 3 would prove nothing: record the row `blocked`, never `pass`. Step 2: the console shows `Module 'UltiEssentials' reloaded.` and no exception stack trace. Step 3: B's sidebar is gone, and B's tab list now shows both A and B with the gray `[UAT] ` prefix although neither account rejoined — B is back on the main scoreboard; each `up_` team still names its player. Step 4: after the cleanup reload, listing each `up_` team shows no members, and `team list` then shows no `up_` team | java-client | |
| ultiessentials.lifecycle.removed-key-warning | `UltiEssentials` loaded and started at least once, so `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml` exists; the server console, or `logs/latest.log`, is readable | 1. Stop the server. In `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml` set `cooldown: 5` in the existing `features:` → `wild:` section, and add a `recall:` section with `enabled: false` under `features:`, indented like the file's other sections. Start the server and read every console line logged while `UltiEssentials` enables. 2. Delete the `recall:` section again, set `cooldown` back to `60`, save, run `ul reload UltiEssentials` from the console, and read every console line the reload logs. | Step 1: exactly one WARN line from this module mentions a removed setting — it contains `'features.recall.enabled' in config/essentials.yml has no effect` and `UltiKits/UltiEssentials#53`; NO line calls `features.wild.cooldown` removed or without effect, because it is a live key (the control that the check lists only removed keys); the module still enables. If the recall line does not appear, record `failed`, not `passed`: the positive half is what makes step 2 mean anything. Step 2: the reload logs the framework's `Module 'UltiEssentials' reloaded.` line and NO line containing `has no effect` — the warning follows the file, it is not printed unconditionally (UltiKits/UltiEssentials#27) | server | |
| ultiessentials.lifecycle.unload-tasks | `language: en` in `plugins/UltiTools/config.yml`; `UltiEssentials` loaded; a copy of this module's JAR kept OUTSIDE the server directory before this row runs, because the uninstall deletes it from `plugins/UltiTools/plugins/` and the cleanup step puts it back; in `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`, BEFORE the server's last restart: `features.scheduled-commands.enabled: true` (NOT the shipped `false`), `features.scheduled-commands.commands` set to the single entry `20:say UAT43 tick` (NOT the shipped default: a 20-second interval so three intervals fit inside this row's observation window, and a marker string no other plugin emits), `features.scoreboard.enabled: true` and `features.scoreboard.auto-enable: true` (both shipped defaults), `features.nameprefix.enabled: true` (NOT the shipped `false`), `features.nameprefix.prefix-format: "&7[UAT] "` (NOT the shipped default: a literal with no placeholder, so the prefix depends on neither Vault nor PlaceholderAPI) and `features.nameprefix.suffix-format: ""`; one account A, joined AFTER that restart and online for at least 5 seconds, so it has both a sidebar and a name-prefix team; `/upm` and `team` run from the server console (both require op) | 1. From the console run `team list`, then `team list` on the `up_` team it shows; as A, look at the screen; read the console for the next 45 seconds. 2. From the console run `upm uninstall UltiEssentials`. 3. Without restarting the server, wait 60 seconds; read the console output since step 2; as A, look at the screen; from the console run `team list`, then `team list` on each `up_` team it shows. 4. Cleanup: stop the server, copy the module JAR back into `plugins/UltiTools/plugins/`, set `features.scheduled-commands.enabled`, `features.scheduled-commands.commands`, `features.nameprefix.enabled` and `features.nameprefix.prefix-format` back to their shipped values, start the server, then from the console run `team list` and `team remove` on each `up_` team it shows. | Step 1: `team list` shows exactly one `up_` team and listing it names A as its only member; A sees the sidebar, titled `Server Info` at the shipped `features.scoreboard.title`, written into the file in English (or the configured title); the console prints `UAT43 tick` at least twice during the 45 seconds. If any one of those three is missing, that task was not running and step 3 would prove nothing: record the row `blocked`, never `pass`. Step 2: the console replies that the module was uninstalled, and prints no exception stack trace. Step 3: NO further `UAT43 tick` line has appeared since step 2, although three 20-second intervals have passed; A's sidebar is gone; the `up_` team is still listed and listing it now shows no members. Before UltiKits/UltiEssentials#43 all three kept running against the uninstalled module until the server was restarted — `UAT43 tick` every 20 seconds, A's sidebar still refreshing, and A still a member of the team. Step 4: after the restart, `team list` shows no `up_` team | java-client | |
| ultiessentials.lifecycle.unload-teleport-warmup | `language: en` in `plugins/UltiTools/config.yml`; `UltiEssentials` loaded; a copy of this module's JAR kept OUTSIDE the server directory, for the same reason as `ultiessentials.lifecycle.unload-tasks`; in `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`, BEFORE the server's last restart: `features.home.enabled: true` and `features.home.cancel-on-move: true` (both shipped defaults) and `features.home.teleport-warmup: 45` (NOT the shipped `3`: a three-second warmup leaves no window to run the uninstall while the countdown is still running; 45 is inside the key's `0..60` range); one account A, online, holding `ultiessentials.home` and NOT holding `ultiessentials.home.nowarmup` (that permission skips the warmup entirely, and then this row has nothing to observe), with one home already set (see `ultiessentials.home.set-default`); `/upm` runs from the server console | 1. As A, move at least 100 blocks away from that home and write down A's coordinates from the debug screen (F3). 2. As A, run `/home`, then watch chat for 5 seconds without moving. 3. Within the next 20 seconds, from the server console run `upm uninstall UltiEssentials`. 4. Without moving and without restarting the server, wait 60 seconds, then read A's chat and A's coordinates from the debug screen. 5. Cleanup: stop the server, copy the module JAR back into `plugins/UltiTools/plugins/`, set `features.home.teleport-warmup` back to `3`, and start the server. | Step 2: a countdown line appears in A's chat about once a second and its number decreases; A has not moved. Under `language: en` the line reads `Teleporting in <n>s, please do not move...` (yellow, `essentials.teleport.warmup`); read its NUMBER. If no countdown line appears, the warmup did not start and step 4 would prove nothing: record the row `blocked`, never `pass`. Step 3: the console replies that the module was uninstalled, and prints no exception stack trace. Step 4: no further countdown line and no teleport line appear in A's chat, and A's coordinates are the ones written down in step 1 — A was NOT moved to the home. Before UltiKits/UltiEssentials#43 the countdown carried on after the uninstall and teleported A to the home when it reached zero | java-client | |
| ultiessentials.lifecycle.unload-reveals-vanished | `language: en` in `plugins/UltiTools/config.yml`; `UltiEssentials` loaded; a copy of this module's JAR kept OUTSIDE the server directory, for the same reason as `ultiessentials.lifecycle.unload-tasks`; `features.hide.enabled: true` (shipped default); a first online player holding `ultiessentials.hide`; a second online player who does NOT hold `ultiessentials.hide.see`; a third player, also without it, who is offline | 1. The first player runs `/hide`, as in `ultiessentials.hide.toggle`; the second player opens the tab list. 2. From the server console run `upm uninstall UltiEssentials`. 3. The second player opens the tab list and looks at the first player's position; then the third player joins and does the same. 4. Cleanup: put the JAR back into `plugins/UltiTools/plugins/` and restart the server. | Step 1: the first player is absent from the second player's tab list — the control that the vanish took effect and that the tab list is being read; if the first player is still listed, record the row `blocked`. Step 3: the first player is listed in, and visible to, the second player again, and is listed for the third player too. Before gate-1 finding WR-01 on UltiKits/UltiEssentials#32 the second player still could not see the first after the uninstall while the third player could | java-client | |

## Data Persistence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.home.persistence | `features.home.enabled: true`; a home named `farm` set (run `ultiessentials.home.set-named` first) | Restart the server, then run `/home farm` | The teleport succeeds to the same coordinates as before the restart | server | |
| ultiessentials.warp.persistence | `features.warp.enabled: true`; a warp named `shop` set (run `ultiessentials.warp.create` first) | Restart the server, then run `/warp shop` | The teleport succeeds to the same coordinates as before the restart | server | |
| ultiessentials.ban.persistence | `features.ban.enabled: true`; an active temporary ban exists (run `ultiessentials.ban.tempban` first) | Restart the server, then run `/banlist` | The same ban entry is still listed, with its remaining time correctly continuing to count down from where it would have been (not reset) | server | |
| ultiessentials.chestlock.persistence | `features.chestlock.enabled: true`; a container locked (run `ultiessentials.lock.lock` first) | Restart the server, then run `/unlock info` on the same container | The lock is still reported, with the same owner name, immediately after boot — before any player has re-locked it this session (confirms `ChestLockService#init`'s `loadCache()` ran at `@PostConstruct`) | server | |
| ultiessentials.storedkey.repair | A **copy** of a data store written by a build EARLIER than this one, holding at least one home and one warp created by that build. Take the copy first (duplicate the store directory, or dump and restore the table into a separate database), point this server at the copy, and leave the original untouched and unread for the whole row — not because of a crash window between the delete and the re-insert, which no longer exists now that one transaction covers the whole repair and either all of it or none of it applies, but because this is the first run of a new repair path against data you did not create. `features.data-repair.enabled: true` (shipped default); `features.home.enabled: true`; `features.warp.enabled: true` | Start the server on the copy, read the start-up log for this module, then run `/delhome <one of those homes>` and `/homes` | The log carries one line per entity type that had records to repair, naming how many were repaired and how many were left alone, followed by a summary line; the pre-existing home then deletes and stops being listed, which it did not before the repair | server | |
| ultiessentials.storedkey.repair.neg-disabled | A second **copy** of the same pre-fix store described in `ultiessentials.storedkey.repair`, untouched by that row; `features.data-repair.enabled: false` | Start the server on this copy and read the start-up log for this module, then run `/delhome <one of the pre-existing homes>` and `/homes` | One log line names `features.data-repair.enabled` and no repair lines appear; `/delhome <that home>` then reports a deletion failure naming the record as still stored — a DIFFERENT message from the one a home that does not exist gets (compare `ultiessentials.home.delete.neg-not-found`) — and `/homes` still lists it. The records were left exactly as they were, which is what the key asks for | server | |
| ultiessentials.storedkey.repair.neg-second-start | `features.home.enabled: true`; the repair has already run once (run `ultiessentials.storedkey.repair` first) | Restart the server and read the start-up log for this module | No repair line appears at all — the second start-up finds nothing to repair and writes nothing | server | |
| ultiessentials.chestlock.break.persistence | `features.chestlock.enabled: true`; the owner has broken their own locked container (run `ultiessentials.chestlock.protect-break.neg-owner` first) | Restart the server, then place a container at the same coordinates and run `/unlock info` on it | The container is reported as not locked — the removal survived the restart rather than the record being reloaded into the cache and locking the new container to the old owner | server | |
| ultiessentials.ban.unban.persistence | `features.ban.enabled: true`; the target has been unbanned (run `ultiessentials.ban.unban` first) | Restart the server, then run `/banlist` and have the target connect | `/banlist` does not list the target and the connection is accepted — the lifted ban survived the restart rather than the record coming back active from the store | server | |
| ultiessentials.home.delete.persistence | `features.home.enabled: true`; the home `farm` has been deleted (run `ultiessentials.home.delete` first) | Restart the server, then run `/homes`, then run `/home farm` | `/homes` does not list `farm`, and `/home farm` reports the home not being found — the deletion survived the restart rather than the record reappearing from the store | server | |
| ultiessentials.warp.delete.persistence | `features.warp.enabled: true`; the warp `shop` has been deleted (run `ultiessentials.warp.delete` first) | Restart the server, then run `/warps`, then run `/warp shop` | `/warps` does not list `shop`, and `/warp shop` reports the warp not being found — the deletion survived the restart rather than the record reappearing from the store | server | |
| ultiessentials.back.not-persisted | `features.back.enabled: true`; the player has a recorded `/back` location (run `ultiessentials.back.track` first), and does NOT quit before the restart | Restart the server WITHOUT the player quitting first, then rejoin and run `/back` | Chat shows the same translated `No location to return to` line as `ultiessentials.back.teleport.neg-no-location` -- the location did not survive the JVM restart, confirming the map is truly in-memory-only rather than merely quit-cleared | server | |
| ultiessentials.hide.not-persisted | `features.hide.enabled: true`; the player is currently vanished (run `ultiessentials.hide.toggle` first), and does NOT quit before the restart | Restart the server WITHOUT the player quitting first, then rejoin | The player is fully visible to everyone on rejoin, with no `/hide` command run — vanish state did not survive the JVM restart | server | |

## Configuration

One row per `@ConfigEntity` class (D-06's config-per-file rule), not per key: `essentials.yml`
(59 keys), `spawn.yml` (8 keys), `lobby.yml` (6 keys), `motd.yml` (3 keys), `tabbar.yml` (2 keys)
— 78 keys total. `FEATURES.md`'s `## Configuration` heading carries the same two numbers and is
the authority for them: reconcile against it rather than trusting the figures here, which went
stale once already when `features.data-repair.enabled` was added and left this row telling testers
to confirm 59 of 60 keys — a count that passes while the newly added, data-mutating safety switch
is the one omitted. Each row confirms
every key in the file is present at its `FEATURES.md`-documented default, then flips one or more
representative keys and observes the behaviour follow. `FEATURES.md` no longer lists any key in
these files as having no observable effect: of the four it used to (UltiKits/UltiEssentials#27),
the two `/ban`/`/unban` broadcast switches are now read and are exercised by
`ultiessentials.ban.ban.neg-broadcast-off` and `ultiessentials.ban.unban.neg-broadcast-off`,
`features.wild.cooldown` is now `/wild`'s cooldown and is exercised by the
`ultiessentials.wild.teleport.neg-cooldown-*` rows, and `features.recall.enabled` was removed — the
`essentials-yml` row below checks that it does not appear in a fresh file.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.config.essentials-yml | Fresh `config/essentials.yml` at its shipped default | Load the file; confirm all 59 keys listed under `FEATURES.md`'s `## Configuration` section are present at their documented defaults (including `features.wild.cooldown: 60`), and that `features.recall.enabled`, removed in 6.3.0, does not appear in the file (UltiKits/UltiEssentials#27); then set `features.heal.enabled: false` (default true), restart, and confirm `ultiessentials.heal.self` (see `## Player Status & Utility`) now shows the translated `This feature is disabled` line (`HealCommand.java:25`) instead of healing; separately set `features.home.default-max-homes: 1` (default 3) and confirm a second `/sethome` attempt now hits the limit at count 1, not 3. | All 59 keys present at their documented defaults, and the removed key absent, before any change; after the changes, `/heal` is refused while disabled, and the home limit is enforced at the new, lower value | protocol | |
| ultiessentials.config.spawn-yml | Fresh `config/spawn.yml` at its shipped default | Load the file; confirm all 8 keys are present at their documented defaults; then set `spawn.location.x: 100` (default 0.0), restart, and confirm `/spawn` now teleports to X=100 rather than X=0 | All 8 keys present at their documented defaults before the change; after the change, `/spawn` teleports to the new X coordinate | server | |
| ultiessentials.config.lobby-yml | Fresh `config/lobby.yml` at its shipped default | Load the file; confirm all 6 keys are present at their documented defaults; then set `lobby.location.y: 100` (default 64.0), restart, and confirm `/lobby` now teleports to Y=100 | All 6 keys present at their documented defaults before the change; after the change, `/lobby` teleports to the new Y coordinate | server | |
| ultiessentials.config.motd-yml | Fresh `config/motd.yml` at its shipped default | Load the file; confirm all 3 keys are present at their documented defaults; then set `motd.line1` to a distinguishable value, restart, and ping the server's status | All 3 keys present at their documented defaults before the change; the server-list MOTD's first line shows the new, distinguishable value | os-input | |
| ultiessentials.config.tabbar-yml | Fresh `config/tabbar.yml` at its shipped default | Load the file; confirm both keys are present at their documented defaults (the shipped header/footer text, written in the server's language -- see `## Config text materializer` below); then set `tabbar.header` to a distinguishable value, restart, and rejoin | Both keys present at their documented defaults before the change; the joining player's tab-list header shows the new, distinguishable value | java-client | |

## Language

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.i18n.language | `language: en` in the framework's `plugins/UltiTools/config.yml`, set once for the session before the start; before the start, `plugins/UltiTools/pluginConfig/UltiEssentials/lang/` was moved into the session backup directory so this jar's catalogues are extracted fresh (an upgraded install never refreshes an already-extracted language file, UltiKits/UltiTools-Reborn#459); `features.home.enabled: true` and `features.tpa.enabled: true` (shipped defaults); the tester online with no home named `uat` | The tester runs `/sethome uat`, then `/tpa <own username>`, then `/delhome uat` | Chat shows, in order: `Home set! (uat)`, `Current homes: <n>/<max>`, `You cannot send a teleport request to yourself`, `Home deleted! (uat)`; none of the lines these commands printed contains Chinese text (UltiKits/UltiEssentials#26) | server | |

## Config text materializer (maintainer decision 2026-09-25, UltiKits/UltiEssentials#26)

Supersedes ruling 2026-09-24 (d). The scoreboard title and lines, the scheduled-command text, the
death-punishment command text (`config/essentials.yml`), the tab-list header and footer
(`config/tabbar.yml`) and the two MOTD lines (`config/motd.yml`) are written into the file in the
server's language when the module starts, and the file is what the module shows — not resolved from
the language file at read time. A setting still at a default an earlier version shipped, or at this
jar's text for it in any language, follows `language` at start-up and on a bare `/ul reload`; a
customised value is kept byte for byte. A blank `features.scoreboard.title` is refused by the
framework (`@NotEmpty`); a blank `features.scoreboard.lines`, `tabbar.header` or `tabbar.footer` is
the operator's way to show nothing and is never materialized. These rows are named by the phase's
config-text convention (`<prefix>.config.<file stem>.materialize-*`, here after `essentials.yml`, the
first of the three files) rather than by a plain `FEATURES.md` ID, and sit next to this module's other lifecycle rows rather than in
`## Configuration`, whose intro says one row per `@ConfigEntity`, not per behaviour.
`…materialize-switch` runs straight after `…materialize-fresh` on the still-running server and
restores `config.yml`, the three module files and `lang/` at its end; `…materialize-upgrade` is
independent and records its own `config/essentials.yml`/`config/tabbar.yml`/`config/motd.yml`
backups. Budget: `…materialize-fresh` adds one start and a join plus a status ping;
`…materialize-switch` two bare `ul reload` runs; `…materialize-upgrade` one start, a join and a
ping, plus a second start to confirm the SHA-256 is unchanged.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.config.essentials.materialize-fresh | `language: en` in the framework's `config.yml`, set once for the session before the start; the server is stopped; `plugins/UltiTools/pluginConfig/UltiEssentials/config/essentials.yml`, `.../config/tabbar.yml`, `.../config/motd.yml` and `.../lang/` are moved to a backup directory outside the server (SHA-256 of each of the three config files recorded), so all three are written fresh; `features.scoreboard.enabled: true`, `features.tab-bar.enabled: true` and `features.motd.enabled: true` (all shipped defaults) | 1. Start the server. 2. Read `features.scoreboard.title`, `features.scoreboard.lines`, `features.scheduled-commands.commands` and `features.deathpunish.command.commands` in `config/essentials.yml`, `tabbar.header` and `tabbar.footer` in `config/tabbar.yml`, and `motd.line1` and `motd.line2` in `config/motd.yml`. 3. Join; run `/scoreboard on`; read the tab-list header/footer and the sidebar. 4. Ping the server's status from a client's server list | Step 2, exactly, no Chinese text in any of the eight: `features.scoreboard.title: '&6&lServer Info'`; `features.scoreboard.lines`: `'&7Welcome, &e%player_name%'`, `'&7'`, `'&6Online: &f%online_players%/%max_players%'`, `'&6World: &f%player_world%'`, `'&7'`, `'&6Health: &c%player_health%'`, `'&6Food: &a%player_food%'`, `'&6Level: &e%player_level%'`, `'&7'`, `'&ewww.example.com'`; `features.scheduled-commands.commands`: `300:say Server is online!` (written unquoted) and `'600:broadcast &cReminder: follow server rules!'`; `features.deathpunish.command.commands`: `say {PLAYER} died!` (written unquoted); `tabbar.header: '&6=== Server Name ==='`; `tabbar.footer: '&7Online: &e%online%&7/&e%max%'`; `motd.line1: '&6Welcome to our server!'`; `motd.line2: '&7Powered by UltiTools'`. Step 3: the tab list and sidebar show exactly that text, color-coded, with `%online%`/`%max%`/PlaceholderAPI-or-fallback substitution applied. Step 4: the MOTD shown is the two configured lines joined by a newline. Leave the server running and the files as written: `…materialize-switch` continues from this state | server | |
| ultiessentials.config.essentials.materialize-switch | `ultiessentials.config.essentials.materialize-fresh` passed and the three files still hold the English text they wrote, except `tabbar.header` set by hand to `'&aMy Custom Header'` (an operator's own value); the server is still running with `language: en` (run this row straight after `ultiessentials.config.essentials.materialize-fresh`, before its backups are restored); `plugins/UltiTools/config.yml` backed up with its SHA-256 | 1. Set `language: zh` in `plugins/UltiTools/config.yml`, save, and run bare `/ul reload` from the console (not `/ul reload UltiEssentials`, which does not re-read `language`). 2. Read `features.scoreboard.title` and `motd.line1` in their files, and `tabbar.header`. 3. Set `language: en`, save, run bare `/ul reload` again, and read the same three. 4. Cleanup: stop the server; restore `plugins/UltiTools/config.yml` from its backup and `config/essentials.yml`, `config/tabbar.yml`, `config/motd.yml` and `lang/` from the backups `…materialize-fresh` took (SHA-256 of each config file read back) | Step 2: read as YAML, `features.scoreboard.title` is `&6&l服务器信息` (the jar's `essentials.scoreboard.default_title` in `lang/zh.json`, the Chinese text earlier versions shipped) and `motd.line1` is `&6欢迎来到我们的服务器!` (the jar's `essentials.motd.default_line1` in `lang/zh.json`); `tabbar.header` is still `'&aMy Custom Header'`. Step 3: `features.scoreboard.title` is `'&6&lServer Info'` and `motd.line1` is `'&6Welcome to our server!'` again; `tabbar.header` is still `'&aMy Custom Header'` — an untouched value follows the switch in both directions, an edited one never does | server | |
| ultiessentials.config.essentials.materialize-upgrade | `language: en` in the framework's `config.yml`; the server is stopped; `config/essentials.yml`, `config/tabbar.yml` and `config/motd.yml` hold exactly what UltiEssentials 1.0.0 wrote (the shipped defaults, the shared server's state), except `features.scoreboard.title` set by hand to `'&bMy Server'` (an operator's own value); a byte copy of each of the three files saved outside the server with its SHA-256 | 1. Start the server, record the SHA-256 of the three config files, and read the same eight keys `…materialize-fresh` names. 2. Join; run `/scoreboard on`; read the tab list and sidebar; ping the server's status. 3. Stop the server and start it again, then compute the SHA-256 of the three files again. 4. Cleanup: stop the server and restore the three byte copies (SHA-256 of each read back) | Step 1: every shipped default is rewritten to the exact English text `…materialize-fresh` quotes, except `features.scoreboard.title`, still `'&bMy Server'`; the three files were saved. Step 2: the sidebar, tab list and MOTD show exactly the files' text. Step 3: all three SHA-256 values equal the ones recorded in step 1 (a second start with the same language writes nothing) | server | |
