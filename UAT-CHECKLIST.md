# UltiEssentials — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only, as
  `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
- **Layer**, copied verbatim from Laojun's own `ultitools-real-client-uat` skill so no translation
  step exists at dispatch time: `protocol`, `java-client`, `os-input`, `pixel`, `server`, `human`.
- **Human-authenticated-session rows (D-27b):** a row whose Steps can only be exercised through the
  maintainer's own authenticated UltiCloud panel session carries the fixed Preconditions phrase
  `maintainer-authenticated UltiCloud panel session (personal credentials)` and Layer `human`. This
  module has no panel-capability rows of its own, so no row below is currently affected; the
  convention is stated here for template consistency with the framework's checklist.
- **Expected** must name an observable truth — an exact chat line, a log line, a database row, an
  inventory slot — and never the words "it works". Per D-02 (English only) and the monorepo CJK
  scan, no row's Expected reproduces a literal Chinese character even when the actual runtime text
  IS Chinese — such rows instead say the text is hardcoded-Chinese-regardless-of-language and cite
  the exact source file and line to read the characters from, following the same pattern the
  framework's own `FEATURES.md`/`UAT-CHECKLIST.md` and UltiChat's `UAT-CHECKLIST.md` already apply
  to their own hardcoded-non-English defects.
- **Covers** back-references a Phase 9 GUI-excluded class name; left blank when no such class
  applies. UltiEssentials is not one of the nine modules in Phase 9's GUI-exclusion register
  (confirmed by reading `.planning/phases/09-module-ecosystem-readiness-and-test-coverage/
  gui-exclusions/` directly — no `UltiEssentials.md` file exists there and no file in that
  directory names UltiEssentials), so every row below leaves `Covers` blank.
- A row whose Preconditions name a prior row must appear after that row in file order — asserted
  mechanically: for every row, every checklist ID cited in its Preconditions cell must have a
  strictly smaller line number in this file than the row citing it (sweep class 8, D-27a).
- **Config-per-file rule (D-06):** one checklist row per `@ConfigEntity`-annotated class, never one
  row per key. A config-per-file row's ID is suffixed `-yml` (e.g. `ultiessentials.config.
  essentials-yml`) and aggregates every per-key `ultiessentials.config.<file-stem>.*` row for that
  file rather than citing a single one of them.
- **Shipped default language is `zh`.** Every row below whose Expected quotes a literal in-game or
  console line carries the precondition `language: en` set in `plugins/UltiTools/config.yml`, so
  the observed line matches this document's English-only text exactly. **The overwhelming majority
  of this module's messages are hardcoded-Chinese regardless of this setting** — see
  `ultiessentials.i18n-coverage` in `FEATURES.md`'s `## Data Persistence` section for the full
  scope. Every row whose live message is affected by that gap still sets `language: en` as its
  precondition (to demonstrate the setting has no effect on that specific line) and describes the
  observed text as "hardcoded-Chinese, unaffected by `language: en`" rather than quoting it.
- **Two different "teleport success" messages exist for the same logical outcome, depending on
  path.** `TeleportService`'s own warmup-countdown `BukkitRunnable` sends the TRANSLATED key
  `teleport_success` ("§aTeleport successful!") when a WARMUP completes. `HomeCommand`/
  `WarpCommand`'s own inline `switch` statements instead send a DIFFERENT, untranslated Chinese
  literal for the SAME `TeleportResult.SUCCESS` outcome on an INSTANT (no-warmup) teleport. A
  player with `ultiessentials.home.nowarmup` therefore sees a different-language success message
  than one without it, for the identical command. Both variants are asserted below where relevant.

## Teleportation & World Presence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.spawn.teleport | `language: en`; `features.spawn.enabled: true` (shipped default); `config/spawn.yml` at its shipped default (`world: "world"`, a world that is loaded) | Run `/spawn` | The player is teleported to the configured spawn coordinates; chat shows the translated `Teleported to spawn` line (`SpawnCommand.java:39` -- that specific key IS present in `lang/en.json`, unlike this command's own `handleHelp` text) | server | |
| ultiessentials.spawn.teleport.neg-disabled | Same as `ultiessentials.spawn.teleport`, except `features.spawn.enabled: false` | Run `/spawn` | Chat shows `This feature is disabled` (§c, `feature_disabled` — a translated key); the player is not moved | server | |
| ultiessentials.spawn.set | `language: en`; `features.spawn.enabled: true` | Stand at a distinguishable position, run `/setspawn`, then restart the server and run `/spawn` | Chat shows the translated `Spawn point set` line (`SetSpawnCommand.java:36` -- translated); the later `/spawn` teleports to the SAME position, confirming `config/spawn.yml` was written to disk, not merely held in memory | server | |
| ultiessentials.spawn.on-respawn | `features.spawn.enabled: true`; `spawn.teleport-on-respawn: true` (shipped default); the player's normal bed/anchor respawn point is somewhere OTHER than the configured spawn location | Die and respawn | The player respawns at the configured spawn coordinates, not their bed/anchor respawn point | server | |
| ultiessentials.spawn.on-respawn.neg-disabled | Same as `ultiessentials.spawn.on-respawn`, except `spawn.teleport-on-respawn: false` | Die and respawn | The player respawns at their normal bed/anchor respawn point (or the world spawn if none set), unaffected by `config/spawn.yml` | server | |
| ultiessentials.spawn.on-first-join | `features.spawn.enabled: true`; `spawn.teleport-on-first-join: true` (shipped default); a player UUID that has genuinely never joined this server before (`Player#hasPlayedBefore()` false) | The player joins for the first time | The player is teleported to the configured spawn coordinates immediately on join | server | |
| ultiessentials.lobby.teleport | `language: en`; `features.lobby.enabled: true` (shipped default); `config/lobby.yml` at its shipped default | Run `/lobby` | The player is teleported to the configured lobby coordinates; chat shows the translated `Teleported to lobby` line (`LobbyCommand.java:39` -- translated) | server | |
| ultiessentials.lobby.teleport.neg-disabled | Same as `ultiessentials.lobby.teleport`, except `features.lobby.enabled: false` | Run `/hub` (alias) | Chat shows `This feature is disabled` (§c, `feature_disabled`); the player is not moved | server | |
| ultiessentials.lobby.set | `language: en`; `features.lobby.enabled: true` | Stand at a distinguishable position, run `/setlobby`, then restart the server and run `/lobby` | Chat shows the translated `Lobby set` line (`SetLobbyCommand.java:36` -- translated); the later `/lobby` teleports to the SAME position, confirming `config/lobby.yml` was written to disk | server | |
| ultiessentials.wild.teleport | `language: en`; `features.wild.enabled: true`; `features.wild.min-range: 100`, `features.wild.max-range: 10000` (shipped defaults); the player is not currently on `/wild`'s cooldown | Run `/wild` | Chat first shows the translated `Finding a safe location...` line (`WildCommand.java:49` -- translated), then the player is teleported to a location within 100-10000 blocks of their prior position, standing on a solid, non-lava/water block with clear space above; the exact target coordinates appear in the translated `Randomly teleported to X:<x> Y:<y> Z:<z>` line (`WildCommand.java:64` -- translated) | server | |
| ultiessentials.wild.teleport.neg-cooldown | Same as `ultiessentials.wild.teleport`, run immediately again within 60 seconds of the prior `/wild` | Run `/wild` a second time inside 60 seconds | The command is rejected by the framework's own `@CmdCD` cooldown validator (a cooldown-remaining message), NOT by `WildCommand`'s own logic; the player is not moved. Independently confirms `features.wild.cooldown` (declared 60 by shipped default) plays no part — the enforcement is the hardcoded `@CmdCD(60)` regardless of that key's value (UltiKits/UltiEssentials#27) | server | |
| ultiessentials.back.teleport | `language: en`; `features.back.enabled: true` (shipped default); the player has run at least one COMMAND- or PLUGIN-caused teleport since their last `/back` (run `ultiessentials.spawn.teleport` first) | Run `/back` | The player is teleported to the position they were at immediately before the tracked teleport; chat shows the translated `Teleported to previous location` line (`BackCommand.java:56` -- translated) | server | |
| ultiessentials.back.teleport.neg-no-location | `language: en`; `features.back.enabled: true`; a freshly-joined player who has never triggered a COMMAND/PLUGIN teleport this session | Run `/back` | Chat shows the translated `No location to return to` line (`BackCommand.java:51` -- translated); the player is not moved | server | |
| ultiessentials.back.track | `features.back.enabled: true` | Run `ultiessentials.spawn.teleport`, noting the exact position beforehand, then immediately run `ultiessentials.back.teleport` | The player returns to the EXACT position recorded before the `/spawn` teleport (not merely "near" it) — confirms the `PLUGIN`-cause teleport this plugin's own `/spawn` performs is tracked, not only vanilla `COMMAND`-cause teleports | server | |
| ultiessentials.back.cleanup-on-quit | `features.back.enabled: true`; the player has a recorded back-location (run `ultiessentials.back.track` first) | Quit and rejoin the server, then run `/back` | Chat shows the same translated `No location to return to` line as `ultiessentials.back.teleport.neg-no-location` -- the recorded location did not survive the quit | server | |

## Homes

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.home.set-default | `language: en`; `features.home.enabled: true` (shipped default); the player has no home named `home` yet | Run `/sethome` | Chat shows a hardcoded-Chinese "home set" success line (read `SetHomeCommand.java:48`) followed by a hardcoded-Chinese count line (`:51`) reading `1/3` (shipped `home.default-max-homes`) | server | |
| ultiessentials.home.set-named | `language: en`; `features.home.enabled: true`; the player has no home named `farm` yet, and holds fewer than 3 homes total (run `ultiessentials.home.set-default` first -- `/sethome farm` alone does not also create a `home`-named entry) | Run `/sethome farm` | Chat shows the same "home set" success line naming `farm`; `/homes` (see `ultiessentials.home.list`) now lists both `home` and `farm` | server | |
| ultiessentials.home.set-named.neg-limit | `language: en`; `features.home.enabled: true`; the player already holds exactly `home.default-max-homes` (3, shipped default) DISTINCT home names and holds no `ultiessentials.home.max.*`/`.unlimited` permission (run `ultiessentials.home.set-default` and `ultiessentials.home.set-named` first, then set one more distinctly-named home to reach 3) | Run `/sethome fourth` | Chat shows a hardcoded-Chinese "home limit reached" line (read `SetHomeCommand.java:57`) immediately followed by a hardcoded-Chinese "use /delhome to remove one" line (`:58`); no fourth home is created — `/homes` still lists exactly 3 | server | |
| ultiessentials.home.teleport-default | `language: en`; `features.home.enabled: true`; a home named `home` exists (run `ultiessentials.home.set-default` first); `features.home.teleport-warmup: 3` (shipped default); the player does NOT hold `ultiessentials.home.nowarmup` | Run `/home` | Chat first shows a hardcoded-Chinese warmup-started line (read `HomeCommand.java:76`), then after ~3 seconds the player is teleported to the `home` location and chat shows the TRANSLATED `§aTeleport successful!` line (`teleport_success`, sent by `TeleportService`, NOT `HomeCommand`'s own switch — see the Conventions note on the two different success messages) | server | |
| ultiessentials.home.teleport-default.neg-nowarmup | Same as `ultiessentials.home.teleport-default`, except the player holds `ultiessentials.home.nowarmup` | Run `/home` | The player is teleported to `home` INSTANTLY, with no warmup countdown; chat shows a DIFFERENT, hardcoded-Chinese success line (read `HomeCommand.java:73`) than the translated one seen in the warmup case | server | |
| ultiessentials.home.teleport-named | `language: en`; `features.home.enabled: true`; a home named `farm` exists (run `ultiessentials.home.set-named` first) | Run `/home farm` | The player is teleported to the `farm` location, with the same warmup/instant success-message split as `ultiessentials.home.teleport-default` depending on `ultiessentials.home.nowarmup` | server | |
| ultiessentials.home.teleport-default.neg-none-set | `language: en`; `features.home.enabled: true`; a player with zero homes set | Run `/home` | Chat shows a hardcoded-Chinese "you have no homes set" line (read `HomeCommand.java:47`); the player is not moved | server | |
| ultiessentials.home.teleport-named.neg-not-found | `language: en`; `features.home.enabled: true`; no home named `does-not-exist` exists for the player | Run `/home does-not-exist` | Chat shows a hardcoded-Chinese "home not found" line (read `HomeCommand.java:79`); the player is not moved | server | |
| ultiessentials.home.delete | `language: en`; `features.home.enabled: true`; a home named `farm` exists (run `ultiessentials.home.set-named` first) | Run `/delhome farm` | Chat shows a hardcoded-Chinese "home deleted" line naming `farm` (read `DelHomeCommand.java:39`); `/homes` (see `ultiessentials.home.list`) no longer lists `farm` | server | |
| ultiessentials.home.delete.neg-not-found | `language: en`; `features.home.enabled: true`; no home named `does-not-exist` exists | Run `/delhome does-not-exist` | Chat shows a hardcoded-Chinese "home not found" line (read `DelHomeCommand.java:41`) | server | |
| ultiessentials.home.list | `language: en`; `features.home.enabled: true`; the player holds at least one home (run `ultiessentials.home.set-default` first) | Run `/homes` | Chat shows a hardcoded-Chinese header line naming the count and limit (e.g. `1/3`, read `HomesCommand.java:42`), then one line per home showing its name, world, and integer-truncated coordinates, then a closing `======...` divider (`:63`) | server | |
| ultiessentials.home.list.neg-empty | `language: en`; `features.home.enabled: true`; a player holding zero homes | Run `/homes` | The header line still shows `0/3`, followed immediately by a hardcoded-Chinese "no homes set" line (read `HomesCommand.java:45`) and a hardcoded-Chinese "use /sethome" hint line (`:46`) — no home entry lines | server | |

## Warps

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.warp.create | `language: en`; `features.warp.enabled: true` (shipped default); no warp named `shop` exists yet | Run `/setwarp shop` | Chat shows a hardcoded-Chinese "warp created" line naming `shop` (read `SetWarpCommand.java:52`) | server | |
| ultiessentials.warp.create.neg-exists | `language: en`; `features.warp.enabled: true`; a warp named `shop` already exists (run `ultiessentials.warp.create` first) | Run `/setwarp shop` again | Chat shows a hardcoded-Chinese "warp already exists" line naming `shop` (read `SetWarpCommand.java:56`); the ORIGINAL warp's coordinates are unchanged (confirm via `/warp shop`) | server | |
| ultiessentials.warp.create-with-permission | `language: en`; `features.warp.enabled: true`; no warp named `vip` exists yet | Run `/setwarp vip vip.warp.access` | Chat shows the same "warp created" line naming `vip` followed by a hardcoded-Chinese "(permission: vip.warp.access)" suffix on the SAME line (read `SetWarpCommand.java:49-50`) | server | |
| ultiessentials.warp.teleport | `language: en`; `features.warp.enabled: true`; a warp named `shop` exists with no access permission (run `ultiessentials.warp.create` first); `features.warp.teleport-warmup: 3` (shipped default); the player does NOT hold `ultiessentials.warp.nowarmup` | Run `/warp shop` | Chat first shows a hardcoded-Chinese warmup line (read `WarpCommand.java:42`), then after ~3 seconds the player is teleported to `shop`'s coordinates with the TRANSLATED `§aTeleport successful!` line — same warmup/instant success-message split noted in `## Homes` | server | |
| ultiessentials.warp.teleport.neg-no-permission | `language: en`; `features.warp.enabled: true`; a warp named `vip` exists requiring `vip.warp.access` (run `ultiessentials.warp.create-with-permission` first); the player does NOT hold `vip.warp.access` | Run `/warp vip` | Chat shows a hardcoded-Chinese "no permission for this warp" line (read `WarpCommand.java:51`); the player is not moved | server | |
| ultiessentials.warp.teleport.neg-not-found | `language: en`; `features.warp.enabled: true`; no warp named `does-not-exist` | Run `/warp does-not-exist` | Chat shows a hardcoded-Chinese "warp not found" line naming `does-not-exist` (read `WarpCommand.java:45`) | server | |
| ultiessentials.warp.delete | `language: en`; `features.warp.enabled: true`; a warp named `shop` exists (run `ultiessentials.warp.create` first) | Run `/delwarp shop` | Chat shows a hardcoded-Chinese "warp deleted" line naming `shop` (read `DelWarpCommand.java:37`); `/warps` (see `ultiessentials.warp.list`) no longer lists `shop` | server | |
| ultiessentials.warp.delete.neg-not-found | `language: en`; `features.warp.enabled: true`; no warp named `does-not-exist` | Run `/delwarp does-not-exist` | Chat shows a hardcoded-Chinese "warp not found" line (read `DelWarpCommand.java:39`) | server | |
| ultiessentials.warp.list | `language: en`; `features.warp.enabled: true`; a warp named `shop` (no access permission) exists (run `ultiessentials.warp.create` first); a second warp named `vip` requiring `vip.warp.access` exists (run `ultiessentials.warp.create-with-permission` first); the listing player does NOT hold `vip.warp.access` | Run `/warps` | Chat shows a hardcoded-Chinese header line (read `WarpsCommand.java:40`), then lists `shop` with world and one-decimal coordinates but NOT `vip` (the player lacks its access permission), then a MIXED footer line reading translated `Total: ` (`:57` -- present in `lang/en.json`) immediately followed by the count `1` and a hardcoded-Chinese suffix naming the unit (`:58` -- NOT present, unlike the `Total: ` prefix on the same line) | server | |
| ultiessentials.warp.list.neg-empty | `language: en`; `features.warp.enabled: true`; zero warps accessible to the listing player | Run `/warps` | Chat shows only a hardcoded-Chinese "no warps available" line (read `WarpsCommand.java:36`) — no header, no entries | server | |

## TPA (Teleport Ask)

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.tpa.send | `language: en`; `features.tpa.enabled: true` (shipped default); the sender is not on the `features.tpa.cooldown` (shipped default 10s); the target is online in the same world (or `features.tpa.allow-cross-world: true`); the target holds no other pending request | Run `/tpa <target>` | The sender sees a hardcoded-Chinese "request sent" line naming the target (read `TpaCommand.java:50`) then a hardcoded-Chinese "waiting for response" line (`:51`); the target sees a hardcoded-Chinese "wants to teleport to you" line prefixed with the sender's name (`:52`) then a hardcoded-Chinese "/tpaccept or /tpdeny" hint (`:53`) | server | |
| ultiessentials.tpa.send.neg-self | `language: en`; `features.tpa.enabled: true` | Run `/tpa <own-username>` | Chat shows a hardcoded-Chinese "cannot send a request to yourself" line (read `TpaCommand.java:56`); no request is created (confirm via `/tpaccept` reporting no pending request) | server | |
| ultiessentials.tpa.send.neg-target-busy | `language: en`; `features.tpa.enabled: true`; the target already holds a pending request from a third player (run `ultiessentials.tpa.send` from a different sender to the same target first) | A second sender runs `/tpa <same-target>` | The second sender sees a hardcoded-Chinese "target has a pending request" line (read `TpaCommand.java:59`); the target's original pending request (from the first sender) is unchanged | server | |
| ultiessentials.tpa.send.neg-cooldown | `language: en`; `features.tpa.enabled: true`; `features.tpa.cooldown: 10` (shipped default); the sender sent a request within the last 10 seconds (run `ultiessentials.tpa.send` immediately before) | The same sender runs `/tpa <a-different-target>` immediately | Chat shows a hardcoded-Chinese "please wait before sending another request" line with the remaining-seconds count appended (read `TpaCommand.java:63`) | server | |
| ultiessentials.tpa.send.neg-cross-world | `language: en`; `features.tpa.enabled: true`; `features.tpa.allow-cross-world: false` (NOT the shipped default — flip it for this row); sender and target in different worlds | Run `/tpa <target>` | Chat shows a hardcoded-Chinese "cross-world teleport not allowed" line (read `TpaCommand.java:66`); no request is created | server | |
| ultiessentials.tpa.send-here | `language: en`; `features.tpa.enabled: true`; same cooldown/busy/world preconditions as `ultiessentials.tpa.send` | Run `/tpahere <target>` | The sender sees the same "request sent"/"waiting" pair as `ultiessentials.tpa.send`; the target sees a hardcoded-Chinese "wants you to teleport to them" line (read `TpaHereCommand.java:52`, distinct Chinese text from `TpaCommand`'s own equivalent line) then the same `/tpaccept`/`/tpdeny` hint | server | |
| ultiessentials.tpa.accept | `language: en`; `features.tpa.enabled: true`; the accepting player holds a pending `/tpa` request from an online sender (run `ultiessentials.tpa.send` first) | The target runs `/tpaccept` | The ORIGINAL SENDER (not the accepter) is teleported to the accepter's location; the accepter sees a hardcoded-Chinese "request accepted" line (read `TpAcceptCommand.java:47`); the sender sees a hardcoded-Chinese "accepted your request" line prefixed with the accepter's name (`:51`) | server | |
| ultiessentials.tpa.accept.neg-none | `language: en`; `features.tpa.enabled: true`; the player holds no pending request | Run `/tpaccept` | Chat shows a hardcoded-Chinese "no pending request" line (read `TpAcceptCommand.java:39`); no teleport occurs | server | |
| ultiessentials.tpa.accept.neg-here | `language: en`; `features.tpa.enabled: true`; the accepting player holds a pending `/tpahere` request instead of `/tpa` (run `ultiessentials.tpa.send-here` first) | The target runs `/tpaccept` | The ACCEPTER (not the original sender) is teleported to the sender's location — the opposite direction from `ultiessentials.tpa.accept`, confirming `TpaType.TPA_HERE` is honoured | server | |
| ultiessentials.tpa.deny | `language: en`; `features.tpa.enabled: true`; the denying player holds a pending request from an online sender (run `ultiessentials.tpa.send` first) | The target runs `/tpdeny` | The denying player sees a hardcoded-Chinese "request denied" line (read `TpDenyCommand.java:46`); the original sender sees a hardcoded-Chinese "request was denied" notification; no teleport occurs for either player | server | |
| ultiessentials.tpa.deny.neg-none | `language: en`; `features.tpa.enabled: true`; the player holds no pending request | Run `/tpdeny` | Chat shows the same "no pending request" line as `ultiessentials.tpa.accept.neg-none` (read `TpDenyCommand.java:39`) | server | |

## Player Status & Utility

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.heal.self | `language: en`; `features.heal.enabled: true` (shipped default); the player's health is below max | Run `/heal` | The player's health is restored to their max-health attribute value; chat shows the translated `Health restored` line (read `lang/en.json` for the exact key/text pair) | server | |
| ultiessentials.heal.other | `language: en`; `features.heal.enabled: true`; an online target below max health; the sender holds `ultiessentials.heal.other` | Run `/heal <target>` | The target's health is restored to max; the sender sees a translated "Restored `<target>`'s health" line; the target separately receives a translated "Your health has been restored" notification | server | |
| ultiessentials.heal.other.neg-offline | `language: en`; `features.heal.enabled: true`; no player named `does-not-exist` is online | Run `/heal does-not-exist` | Chat shows the translated `Player not found or offline` line | server | |
| ultiessentials.feed.self | `language: en`; `features.heal.enabled: true`; the player's food level is below 20 | Run `/feed` | The player's food level is set to 20 and saturation to 20.0; chat shows the translated `Hunger restored` line | server | |
| ultiessentials.feed.other | `language: en`; `features.heal.enabled: true`; an online target below full food; the sender holds `ultiessentials.heal.other` (shared with `/heal`, see FEATURES.md section note) | Run `/feed <target>` | The target's food is set to 20/saturation 20.0; the sender and target each receive the translated feed-restored equivalents of `ultiessentials.heal.other`'s messages | server | |
| ultiessentials.speed.set | `language: en`; `features.speed.enabled: true` (shipped default); `features.speed.max-speed: 10` (shipped default) | Run `/speed 5` | The player's walk speed becomes `0.2 * 5 = 1.0` (capped at 1.0) and fly speed `min(1.0, 0.1*5)=0.5`; chat shows the translated `Speed set to 5` line | server | |
| ultiessentials.speed.set.neg-out-of-range | `language: en`; `features.speed.enabled: true`; `features.speed.max-speed: 10` | Run `/speed 11` | Chat shows the translated `Speed must be between 0-10` line; the player's speed is unchanged | server | |
| ultiessentials.speed.reset | `language: en`; `features.speed.enabled: true`; the player's speed was previously changed (run `ultiessentials.speed.set` first) | Run `/speed reset` | The player's walk speed returns to 0.2 and fly speed to 0.1 (the platform default); chat shows the translated `Speed reset to default` line | server | |
| ultiessentials.fly.toggle-self | `language: en`; `features.fly.enabled: true` (shipped default); the player's flight is currently disallowed | Run `/fly` | `Player#getAllowFlight()` becomes true; chat shows the translated `Flight mode enabled` line | server | |
| ultiessentials.fly.toggle-self.neg-disable | Same as `ultiessentials.fly.toggle-self`, run again with flight currently allowed | Run `/fly` again | `Player#getAllowFlight()` becomes false and the player is also forced out of active flight (`setFlying(false)`); chat shows the translated `Flight mode disabled` line | server | |
| ultiessentials.fly.toggle-other | `language: en`; `features.fly.enabled: true`; an online target; the ACTING player holds ONLY `ultiessentials.fly` (the same node that gates their own `/fly`, NOT a separate elevated node — see UltiKits/UltiEssentials#25) | A player holding only the base `ultiessentials.fly` permission (no admin/moderator grant) runs `/fly <target>` | The command SUCCEEDS — the target's flight is toggled and both players receive the translated notification pair, despite the acting player holding no elevated permission beyond ordinary self-fly access. This is the live demonstration of UltiKits/UltiEssentials#25, not a workflow defect | server | |
| ultiessentials.hide.toggle | `language: en`; `features.hide.enabled: true` (shipped default); the player is currently visible; at least one other online player who does NOT hold `ultiessentials.hide.see` | Run `/hide` | The sender becomes invisible to the other online player (no longer appears in their tab list / is not visible in-world); chat shows the translated `Vanish mode enabled` line | server | |
| ultiessentials.hide.toggle.neg-see-permission | Same as `ultiessentials.hide.toggle`, except the OTHER online player holds `ultiessentials.hide.see` | The vanished player is checked against this second player | The second player CAN still see the vanished player (visibility exemption honoured per-observer) | server | |
| ultiessentials.gamemode.set-self | `language: en`; `features.gamemode.enabled: true` (shipped default) | Run `/gm creative` | The player's game mode becomes CREATIVE; chat shows the translated `Game mode changed to CREATIVE` line | server | |
| ultiessentials.gamemode.set-self.neg-invalid | `language: en`; `features.gamemode.enabled: true` | Run `/gm bogus` | Chat shows the translated `Invalid game mode. Available: 0/s/survival, 1/c/creative, 2/a/adventure, 3/sp/spectator` line; the player's game mode is unchanged | server | |
| ultiessentials.gamemode.set-other | `language: en`; `features.gamemode.enabled: true`; an online target; the sender holds `ultiessentials.gamemode.other` | Run `/gm survival <target>` | The target's game mode becomes SURVIVAL; the sender sees a translated "Changed `<target>`'s game mode to SURVIVAL" line; the target separately sees a translated "Your game mode has been changed to SURVIVAL" line | server | |
| ultiessentials.gamemode.shortcut-creative | `language: en`; `features.gamemode.enabled: true` | Run `/gmc` | The player's game mode becomes CREATIVE; chat shows the same translated line as `ultiessentials.gamemode.set-self` | server | |
| ultiessentials.gamemode.shortcut-spectator | `language: en`; `features.gamemode.enabled: true` | Run `/gmsp` | The player's game mode becomes SPECTATOR; chat shows the translated `Game mode changed to SPECTATOR` line | server | |
| ultiessentials.gamemode.shortcut-survival | `language: en`; `features.gamemode.enabled: true` | Run `/gms` | The player's game mode becomes SURVIVAL; chat shows the translated `Game mode changed to SURVIVAL` line | server | |
| ultiessentials.scoreboard.toggle | `language: en`; the player's sidebar scoreboard is currently off | Run `/scoreboard` | The player's sidebar scoreboard appears, titled per `features.scoreboard.title` and showing `features.scoreboard.lines`; chat ALSO shows a hardcoded-Chinese, no-i18n-translation success line (`ScoreboardCommand.java:34` -- `ScoreboardCommand` has 0 of its 8 messages present in `lang/en.json`, confirmed by a full per-file key diff) | server | |
| ultiessentials.scoreboard.enable | `language: en`; the player's sidebar scoreboard is currently off | Run `/scoreboard on` | The scoreboard appears; chat shows the same hardcoded-Chinese success line as `ultiessentials.scoreboard.toggle` (`ScoreboardCommand.java:48`) | server | |
| ultiessentials.scoreboard.enable.neg-already | Same as `ultiessentials.scoreboard.enable`, run again while already on | Run `/scoreboard on` a second time | The scoreboard remains unchanged; a distinct hardcoded-Chinese "already enabled" line appears (read `ScoreboardCommand.java:43`) rather than the enable-success line | server | |
| ultiessentials.scoreboard.disable | `language: en`; the player's sidebar scoreboard is currently on (run `ultiessentials.scoreboard.enable` first) | Run `/scoreboard off` | The scoreboard disappears (player receives a fresh, empty scoreboard, per `ScoreboardService#disableScoreboard`); chat shows a hardcoded-Chinese success line (`ScoreboardCommand.java:59`) | server | |
| ultiessentials.scoreboard.disable.neg-already | Same as `ultiessentials.scoreboard.disable`, run again while already off | Run `/scoreboard off` a second time | A distinct hardcoded-Chinese "already disabled" line appears (read `ScoreboardCommand.java:54`) rather than the disable-success line | server | |
| ultiessentials.scoreboard.auto-enable-on-join | `features.scoreboard.enabled: true`, `features.scoreboard.auto-enable: true` (both shipped defaults); a player who has never toggled their own scoreboard this session | The player joins the server and waits 1 second | The sidebar scoreboard appears automatically, without the player running any command | server | |
| ultiessentials.scoreboard.disable-on-quit | `features.scoreboard.enabled: true`; `features.scoreboard.auto-enable: false` (so nothing re-adds the player to `enabledPlayers` between quit and rejoin, which would mask the effect this row tests); the player's scoreboard is currently enabled (run `ultiessentials.scoreboard.enable` first) | The player quits, then immediately rejoins and runs `/scoreboard on` | The rejoin's `/scoreboard on` shows the ENABLE-success line (`ultiessentials.scoreboard.enable`'s own line), not the "already enabled" line (`ultiessentials.scoreboard.enable.neg-already`) -- proving `ScoreboardService#enabledPlayers` no longer contained the player's UUID after the quit, since nothing else could have removed it in between | server | |
| ultiessentials.commandalias.rewrite | `language: en`; `features.commandalias.enabled: true` (shipped default); `commandalias.aliases` at its shipped default (includes `gmc: "gamemode creative"`) | As a player NOT holding `ultiessentials.gamemode.self` or `/gamemode` permission, type `/gmc` in chat | The command actually dispatched is `/gamemode creative`, not UltiEssentials' own `/gmc` shortcut — Bukkit's vanilla `/gamemode` permission gate applies, NOT `ultiessentials.gamemode.self`, proving the rewrite happened before permission checking, not after | server | |
| ultiessentials.commandalias.rewrite.neg-disabled | Same as `ultiessentials.commandalias.rewrite`, except `features.commandalias.enabled: false` | Type `/gmc` in chat | UltiEssentials' own `/gmc` shortcut command handles it directly (its own `ultiessentials.gamemode.self` permission gate applies), because no rewrite occurred | server | |
| ultiessentials.cleanup.on-quit | The player has an active `/back` location AND active `/hide` vanish state (run `ultiessentials.back.track` and `ultiessentials.hide.toggle` first) | The player quits | `BackCommand.LAST_LOCATIONS` and `HideCommand.HIDDEN_PLAYERS` both no longer contain the player's UUID immediately after the quit event — confirmed by the subsequent `/back` refusal (`ultiessentials.back.cleanup-on-quit`) and by the player rejoining visible to everyone again with no `/hide` toggle run | server | |

## Player Inspection

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.invsee.view | `language: en`; `features.invsee.enabled: true` (shipped default); an online target other than the sender | Run `/invsee <target>` | The sender's inventory screen opens showing the TARGET's live main inventory (36 slots); chat shows a translated "Viewing `<target>`'s inventory" line; an item moved in this view is reflected in the target's real inventory (live, editable) | server | |
| ultiessentials.invsee.view.neg-offline | `language: en`; `features.invsee.enabled: true`; no player named `does-not-exist` online | Run `/invsee does-not-exist` | Chat shows the translated `Player not found or offline` line; no inventory opens | server | |
| ultiessentials.endersee.view | `language: en`; `features.invsee.enabled: true`; an online target | Run `/endersee <target>` | The sender's inventory screen opens showing the target's live 27-slot ender chest; chat shows a translated "Viewing `<target>`'s ender chest" line | server | |
| ultiessentials.armorsee.view | `language: en`; `features.invsee.enabled: true`; an online target wearing distinguishable armor pieces, other than the sender | Run `/armorsee <target>` | A temporary 9-slot inventory opens showing the target's helmet/chestplate/leggings/boots (in that display order) in slots 0-3 and their off-hand item in slot 5; chat shows a translated "Viewing `<target>`'s equipment" line | server | |
| ultiessentials.armorsee.view.neg-self | `language: en`; `features.invsee.enabled: true` | Run `/armorsee <own-username>` | UNLIKE `ultiessentials.invsee.view.neg-self`'s equivalent, `ArmorseeCommand#armorsee` has no self-target check at all -- the temporary inventory opens showing the sender's OWN armor/off-hand, and the translated "Viewing `<own-username>`'s equipment" line still appears | server | |

## Moderation

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.ban.ban | `language: en`; `features.ban.enabled: true` (shipped default); a target player, online or previously-seen offline, not currently banned | Run `/ban <target>` | If online, the target is kicked immediately with a hardcoded-Chinese, always-Chinese-regardless-of-language kick screen (read `BanService#formatKickMessage`, `BanService.java:296-310`, for the exact text — this method contains no `i18n(...)` call at all); the server broadcasts a ban notice naming the target and the fixed default reason | server | |
| ultiessentials.ban.ban.neg-already-banned | `language: en`; `features.ban.enabled: true`; the target is already actively banned (run `ultiessentials.ban.ban` first) | Run `/ban <same-target>` again | Chat shows a hardcoded-Chinese "already banned" line (read `BanCommand.java:71`); no duplicate ban record or duplicate broadcast occurs | server | |
| ultiessentials.ban.ban-with-reason | `language: en`; `features.ban.enabled: true`; a target not currently banned | Run `/ban <target> griefing` | The target is kicked (if online) with `griefing` shown as the reason in the kick screen; the broadcast names both the target and `griefing` | server | |
| ultiessentials.ban.tempban | `language: en`; `features.ban.enabled: true`; a target not currently banned | Run `/tempban <target> 1h` | The target is kicked (if online) with a "remaining time" section in the kick screen reflecting approximately 1 hour; the broadcast names the target, the fixed default reason, and the formatted duration | server | |
| ultiessentials.ban.tempban.neg-bad-duration | `language: en`; `features.ban.enabled: true` | Run `/tempban <target> notaduration` | Chat shows a hardcoded-Chinese "invalid duration format" line followed by a hardcoded-Chinese format-example line (read `TempBanCommand.java:59-60`, neither present in `lang/en.json`); no ban is created | server | |
| ultiessentials.ban.tempban-with-reason | `language: en`; `features.ban.enabled: true`; a target not currently banned | Run `/tempban <target> 1h griefing` | Same as `ultiessentials.ban.tempban`, with `griefing` shown as the reason | server | |
| ultiessentials.ban.unban | `language: en`; `features.ban.enabled: true`; the target is banned ONLY through this plugin's own record (no separate vanilla `/ban`), run `ultiessentials.ban.ban` first | Run `/unban <target>` | Chat shows a hardcoded-Chinese, plain-success unban line (read `UnbanCommand.java:48-49`); the server broadcasts an unban notice naming the target; the target can now rejoin | server | |
| ultiessentials.ban.unban.neg-not-banned | `language: en`; `features.ban.enabled: true`; the target has never been banned by this plugin nor by the server's vanilla ban list | Run `/unban <target>` | Chat shows a hardcoded-Chinese "not banned" line naming the target (read `UnbanCommand.java:59`) | server | |
| ultiessentials.ban.unban.neg-also-server-banned | `language: en`; `features.ban.enabled: true`; the target is banned by BOTH this plugin's own record AND a separate vanilla `/ban` (run `ultiessentials.ban.ban` on the target, then separately run vanilla `/ban <target>` as an OP) | Run `/unban <target>` | Chat shows a DIFFERENT, TRANSLATED line explicitly stating this plugin's ban was removed but the server's own ban list still bans the player -- `Removed this plugin's ban on <target>, but the server's ban list still bans this player` (`UnbanCommand.java:45-46`, both lines present in `lang/en.json`, unlike the plain-success case at `:48-49`); the broadcast for a plain successful unban does NOT fire; the target still cannot rejoin (vanilla ban still active) | server | |
| ultiessentials.banlist.list | `language: en`; `features.ban.enabled: true`; at least one active ban exists (run `ultiessentials.ban.ban` first) | Run `/banlist` | Chat shows the TRANSLATED header `=== Ban List === (1/1)` (`BanListCommand.java:59-60` -- every message in this class is present in `lang/en.json`, unlike almost every other moderation command in this module), then one entry per active ban naming the player, permanent-or-remaining-time, reason, and operator (all translated), then a translated total-count footer reading `Total: 1 active ban` (`:82-83`) | server | |
| ultiessentials.banlist.list.neg-empty | `language: en`; `features.ban.enabled: true`; zero active bans | Run `/banlist` | Chat shows only the translated `No active bans` line (`BanListCommand.java:49` -- translated) -- no header, no entries | server | |
| ultiessentials.banlist.list-page | `language: en`; `features.ban.enabled: true`; more than 10 active bans exist (create 11+ temp-bans against distinct fictitious names via `/tempban`) | Run `/banlist 2` | Page 2 shows entries 11 onward (up to 20), NOT a repeat of page 1's entries | server | |
| ultiessentials.ban.login-check | `features.ban.enabled: true`; the connecting player's UUID has an active, unexpired ban (run `ultiessentials.ban.ban` on that player first, while they are offline) | The banned player attempts to join | The connection is refused at the login stage (`KICK_BANNED`) with the same hardcoded-Chinese kick screen as `ultiessentials.ban.ban`'s own kick message; the player never reaches the world | server | |
| ultiessentials.whitelist.add | `language: en`; `features.whitelist.enabled: true` (shipped default); a player name `Notch` not currently whitelisted | Run `/wl add Notch` | Chat shows a translated "Added Notch to whitelist" line; `/wl list` (see below) now includes `Notch` | server | |
| ultiessentials.whitelist.add.neg-too-long | `language: en`; `features.whitelist.enabled: true` | Run `/wl add ThisNameIsWayTooLongForMinecraft` (17+ characters) | Chat shows the translated `Player name is too long, maximum 16 characters` line; NO `IllegalArgumentException` is thrown and the server does not error — this row is the regression guard for the crash `Bukkit.getOfflinePlayer` used to throw, fixed per UltiKits/UltiEssentials#17 | server | |
| ultiessentials.whitelist.remove | `language: en`; `features.whitelist.enabled: true`; `Notch` is currently whitelisted (run `ultiessentials.whitelist.add` first) | Run `/wl remove Notch` | Chat shows a translated "Removed Notch from whitelist" line; `/wl list` no longer includes `Notch` | server | |
| ultiessentials.whitelist.remove.neg-overlong-legacy-entry | `language: en`; `features.whitelist.enabled: true`; a whitelist entry exists whose stored name is over 16 characters (simulate by hand-editing `whitelist.json` to add such an entry, then restart) | Run `/wl remove <the-overlong-name>` | The removal SUCCEEDS — `WhitelistCommand#remove` matches against the CURRENTLY whitelisted set by string comparison rather than resolving through `Bukkit.getOfflinePlayer`, so an over-length legacy entry can still be removed even though it could never have been re-added via `/wl add` | server | |
| ultiessentials.whitelist.list | `language: en`; `features.whitelist.enabled: true`; `Notch` is whitelisted (run `ultiessentials.whitelist.add` first) | Run `/wl list` | Chat shows a translated `Whitelist (1): Notch`-shaped line (count and comma-joined names) | server | |
| ultiessentials.whitelist.list.neg-empty | `language: en`; `features.whitelist.enabled: true`; zero whitelisted players | Run `/wl list` | Chat shows the translated `Whitelist is empty` line | server | |
| ultiessentials.whitelist.enable | `language: en`; `features.whitelist.enabled: true`; server whitelist enforcement currently off | Run `/wl on` | `Bukkit.hasWhitelist()` becomes true; chat shows the translated `Whitelist enabled` line; a non-whitelisted player can no longer join | server | |
| ultiessentials.whitelist.disable | `language: en`; `features.whitelist.enabled: true`; server whitelist enforcement currently on (run `ultiessentials.whitelist.enable` first) | Run `/wl off` | `Bukkit.hasWhitelist()` becomes false; chat shows the translated `Whitelist disabled` line | server | |
| ultiessentials.whitelist.status | `language: en`; `features.whitelist.enabled: true`; whitelist currently on with `Notch` the only entry (run `ultiessentials.whitelist.enable` and `ultiessentials.whitelist.add` first) | Run `/wl status` | Chat shows a translated `Whitelist status: Enabled, count: 1`-shaped line | server | |

## Container Locking

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.lock.lock | `language: en`; `features.chestlock.enabled: true` (shipped default); the player looking directly at an unlocked chest within 5 blocks | Run `/lock` | Chat shows a hardcoded-Chinese "locked" success line (read `LockCommand.java:42`); the container can no longer be opened by any other player lacking `ultiessentials.lock.admin` | server | |
| ultiessentials.lock.lock.neg-not-lockable | `language: en`; `features.chestlock.enabled: true`; the player looking at a non-lockable block (e.g. plain stone) within 5 blocks | Run `/lock` | Chat shows a hardcoded-Chinese "this block cannot be locked" line (read `LockCommand.java:45`) | server | |
| ultiessentials.lock.lock.neg-already-locked | `language: en`; `features.chestlock.enabled: true`; a chest already locked by a DIFFERENT player (run `ultiessentials.lock.lock` as a different player first) | Run `/lock` as the second player | Chat shows a hardcoded-Chinese "already locked by someone else" line (read `LockCommand.java:48`); ownership of the lock is unchanged | server | |
| ultiessentials.lock.lock.neg-already-locked-by-you | Same as `ultiessentials.lock.lock`, run a second time by the SAME player who already locked it | Run `/lock` again as the owner | Chat shows a DIFFERENT hardcoded-Chinese "you already locked this" line (read `LockCommand.java:51`), distinct from the other-owner case | server | |
| ultiessentials.lock.unlock | `language: en`; a chest locked by the acting player (run `ultiessentials.lock.lock` first) | Run `/unlock` | Chat shows a hardcoded-Chinese "unlocked" success line (read `UnlockCommand.java:43`); the container can now be opened by anyone again | server | |
| ultiessentials.lock.unlock.neg-not-locked | `language: en`; the looked-at container is not locked | Run `/unlock` | Chat shows a hardcoded-Chinese "not locked" line (read `UnlockCommand.java:46`) | server | |
| ultiessentials.lock.unlock.neg-not-owner | `language: en`; a chest locked by a DIFFERENT player; the acting player does NOT hold `ultiessentials.lock.admin` | Run `/unlock` | Chat shows a hardcoded-Chinese "not the owner" line (read `UnlockCommand.java:49`); the lock is unchanged | server | |
| ultiessentials.lock.unlock.neg-disabled-still-works | `language: en`; `features.chestlock.enabled: false` (NOT the shipped default); a container that was locked while the feature was still enabled | Run `/unlock` | Unlocking still succeeds — `ChestLockService#unlockBlock` has no `chestlock.enabled` check at all, unlike `#lockBlock` (see FEATURES.md `## Container Locking` section note) | server | |
| ultiessentials.lock.info | `language: en`; a chest locked by some player (run `ultiessentials.lock.lock` first) | Run `/unlock info` | Chat shows a hardcoded-Chinese header (read `UnlockCommand.java:68`), then the owner's name (`:69`) and the world+coordinates (`:70-71`) | server | |
| ultiessentials.lock.info.neg-not-locked | `language: en`; the looked-at container is not locked | Run `/unlock info` | Chat shows a hardcoded-Chinese "this container is not locked" line (read `UnlockCommand.java:66`), distinct from the header shown in the positive case | server | |
| ultiessentials.chestlock.protect-interact | `features.chestlock.enabled: true`; a chest locked by a different player; the interacting player does NOT hold `ultiessentials.lock.admin`, and `chestlock.admin-bypass` is irrelevant since they hold no admin permission at all | Right-click the locked chest | The inventory does NOT open; the event is cancelled; the interacting player receives a hardcoded-Chinese message naming the lock owner (read `ChestLockListener.java:63-64`) | server | |
| ultiessentials.chestlock.protect-interact.neg-admin-bypass | Same as `ultiessentials.chestlock.protect-interact`, except the interacting player holds `ultiessentials.lock.admin` AND `features.chestlock.admin-bypass: true` (shipped default) | Right-click the same locked chest | The inventory DOES open normally | server | |
| ultiessentials.chestlock.protect-break | `features.chestlock.enabled: true`; a chest locked by a different player; the breaking player holds neither ownership nor `ultiessentials.lock.admin` | Attempt to break the locked chest | The break is cancelled; the breaking player receives a DIFFERENT hardcoded-Chinese message than the interact-refusal one, naming the owner and stating the container "cannot be broken" (read `ChestLockListener.java:94-95`); the block remains intact and still locked | server | |
| ultiessentials.chestlock.protect-break.neg-owner | Same as `ultiessentials.chestlock.protect-break`, except the breaking player is the lock's owner | Break the chest | The break succeeds; the lock record for that location is removed (confirm via `/unlock info` on a re-placed chest at the same coordinates reporting "not locked") | server | |
| ultiessentials.chestlock.protect-explode-entity | `features.chestlock.enabled: true`; a locked chest adjacent to a TNT block that will explode; a SECOND, UNLOCKED control block of the same breakable material placed at an equal distance from the TNT, in a direction with no other interference, so the explosion's own randomness can be judged against a like-for-like comparison rather than assuming every block in range is destroyed | Detonate the TNT | The unlocked control block IS destroyed by the explosion, but the locked chest at the equal distance survives intact and remains locked -- `ChestLockListener#onEntityExplode` only removes locked locations from `event.blockList()` and makes no guarantee about any other block in range | server | |
| ultiessentials.chestlock.protect-explode-block | `features.chestlock.enabled: true`; a locked chest positioned to be destroyed by a bed/respawn-anchor mismatched-dimension explosion | Trigger the block explosion | The locked chest survives while surrounding blocks are destroyed, same as the entity-explosion case | server | |
| ultiessentials.chestlock.protect-piston-extend | `features.chestlock.enabled: true`; a piston positioned to push a locked SHULKER BOX when extended (a locked CHEST cannot be used for this row: chests, like all standard tile-entity containers, are not piston-movable in vanilla Minecraft at all — the push chain never includes one regardless of this listener, so it can never exercise `ChestLockListener#onPistonExtend`'s own `getBlocks()` check; shulker boxes are Mojang's one deliberate exception and ARE piston-movable, and `ChestLockService`'s own `LOCKABLE_BLOCKS` set includes every shulker-box colour, so a locked shulker box constructs the same protection scenario without asserting a setup the game engine forecloses) | Power the piston to extend | The piston extend event is cancelled entirely — neither the piston head nor any block in its push chain moves, not merely the locked shulker box | server | |
| ultiessentials.chestlock.protect-piston-retract | `features.chestlock.enabled: true`; a sticky piston positioned to pull a locked SHULKER BOX when retracted (same physical impossibility as `ultiessentials.chestlock.protect-piston-extend` above — a chest cannot be pulled by a sticky piston either, for the identical tile-entity reason; a shulker box is piston-movable in both directions) | Power the piston to retract | The piston retract event is cancelled entirely — neither the piston head nor any block in its pull chain moves, not merely the locked shulker box | server | |
| ultiessentials.chestlock.protect-hopper | `features.chestlock.enabled: true`; a hopper feeding directly out of a locked chest | Place items in the locked chest and wait for a hopper cycle | The hopper does not remove any item from the locked chest; items remain in place | server | |

## Server Presentation

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.motd.customize | `features.motd.enabled: true` (shipped default); `config/motd.yml` at its shipped default | Ping the server's status from a client's server list (or an equivalent status query tool) | The displayed MOTD is exactly the two configured lines joined by a newline, color-coded per `&6`/`&7`, NOT the platform's own `motd` from `server.properties` | os-input | |
| ultiessentials.motd.customize.neg-max-players-override | Same as `ultiessentials.motd.customize`, except `motd.max-players: 5` (NOT the shipped `-1`) | Ping the server's status | The displayed max-player count reads `5`, overriding the platform's real configured value | os-input | |
| ultiessentials.tabbar.on-join | `features.tab-bar.enabled: true` (shipped default); `config/tabbar.yml` at its shipped default; at least 2 players online | Join the server as a NEW connection (`TabBarListener#onPlayerJoin` calls `updateTabBar` only for the joining player -- an already-online player pressing Tab after someone else joins does NOT refresh their own header/footer) | The tab list header/footer show the configured, color-coded templates with `%online%`/`%max%` correctly substituted with the current live counts | java-client | |
| ultiessentials.nameprefix.on-join | `features.nameprefix.enabled: true` (NOT the shipped `false` — flip it on for this row); `features.nameprefix.prefix-format` at its shipped default; `features.scoreboard.enabled: false` (NOT the shipped `true` -- REQUIRED for this row: `ScoreboardService#updateScoreboard` calls `player.setScoreboard(manager.getNewScoreboard())` at the scoreboard feature's own 1-second auto-enable delay, which reassigns the player away from the main scoreboard `NamePrefixService` registers its team on at 0.5 seconds, masking the prefix for that player if left at the shipped default); a Vault-compatible permissions plugin providing a `%vault_prefix%` value (or PlaceholderAPI absent, exercising the raw-token fallback) | Join the server and wait 0.5 seconds | The player's name, as shown above their head and in the tab list, carries the configured prefix template; a distinct player's prefix does not affect this one (per-player team entry on the shared main scoreboard) | java-client | |
| ultiessentials.nameprefix.on-quit | Same feature-enabled precondition as `ultiessentials.nameprefix.on-join`; the player has an active prefix (run that row first) | The player quits, then a DIFFERENT observer checks the scoreboard team's entry list | The quitting player's name is no longer a member of any name-prefix team (source-level check: `NamePrefixService#playerTeams` no longer maps their UUID after the quit event — read `NamePrefixListener.java:76-78`) | protocol | |
| ultiessentials.deathpunish.on-death | `features.deathpunish.enabled: true` AND `features.deathpunish.exp.enabled: true` (BOTH NOT the shipped default of `false` -- flip both on explicitly; enabling the parent toggle does not change the `exp.enabled` sub-toggle's own default); `features.deathpunish.exp.percent: 20.0` (shipped default once `exp.enabled` is explicitly turned on); the player has AT LEAST 50 total experience points (so `(int)(total * 0.20)` is comfortably positive and, per `PlayerDeathEvent#getDroppedExp()`'s own vanilla-computed value at that experience level, not clamped to zero by `Math.max(0, event.getDroppedExp() - expLoss)`); the player does NOT hold `ultiessentials.deathpunish.bypass`; the player is not in a whitelisted world (`world_creative`, shipped default, is excluded — die elsewhere) | Die as the player | The player's dropped experience orbs are reduced by `(int)(total_experience * 0.20)` (an integer truncation of 20% of their PRE-DEATH total experience, not 20% of the vanilla-computed drop amount alone -- confirm by comparing to a control death with `features.deathpunish.enabled: false`), CLAMPED to never go below zero even if the computed loss exceeds the vanilla drop amount; the player receives a hardcoded-Chinese punishment-summary line (no i18n call at all — read `DeathPunishListener.java:83`) naming the experience loss | server | |
| ultiessentials.deathpunish.on-death.neg-bypass | Same as `ultiessentials.deathpunish.on-death`, except the player holds `ultiessentials.deathpunish.bypass` | Die as the player | No punishment of any kind applies; the vanilla death behaviour is completely unmodified | server | |
| ultiessentials.deathpunish.on-death.neg-world-whitelist | Same as `ultiessentials.deathpunish.on-death`, except the player dies inside `world_creative` (the shipped `deathpunish.world-whitelist` entry) | Die as the player in `world_creative` | No punishment applies, even though the player holds no bypass permission | server | |

## Scheduled Commands

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.scheduledcommands.run | `features.scheduled-commands.enabled: true` (NOT the shipped `false` — flip it on); `scheduled-commands.commands` at its shipped default (`300:say Server is online!`, `600:broadcast &cReminder: follow server rules!`); restart the server after changing the config so the new value is read at boot (see `## Configuration`'s note on `/ul reload` not refreshing this service, UltiKits/UltiEssentials#28) | Wait 300 seconds after server start | The console/chat shows the `say Server is online!` output; at 600 seconds the `&c`-colored broadcast reminder also appears; both then repeat on the same intervals indefinitely | server | |
| ultiessentials.scheduledcommands.run.neg-malformed-entry | Same base preconditions as `ultiessentials.scheduledcommands.run`, except `features.scheduled-commands.commands` includes one additional malformed entry with no colon (e.g. `"broadcast oops"`) | Restart the server and check the startup log | A warning naming the malformed entry is logged (`ScheduledCommandService#startTasks`'s own `log.warn`); the OTHER, well-formed entries still schedule and run normally — one bad entry does not abort the whole list | server | |

## Data Persistence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.home.persistence | `features.home.enabled: true`; a home named `farm` set (run `ultiessentials.home.set-named` first) | Restart the server, then run `/home farm` | The teleport succeeds to the same coordinates as before the restart | server | |
| ultiessentials.warp.persistence | `features.warp.enabled: true`; a warp named `shop` set (run `ultiessentials.warp.create` first) | Restart the server, then run `/warp shop` | The teleport succeeds to the same coordinates as before the restart | server | |
| ultiessentials.ban.persistence | `features.ban.enabled: true`; an active temporary ban exists (run `ultiessentials.ban.tempban` first) | Restart the server, then run `/banlist` | The same ban entry is still listed, with its remaining time correctly continuing to count down from where it would have been (not reset) | server | |
| ultiessentials.chestlock.persistence | `features.chestlock.enabled: true`; a container locked (run `ultiessentials.lock.lock` first) | Restart the server, then run `/unlock info` on the same container | The lock is still reported, with the same owner name, immediately after boot — before any player has re-locked it this session (confirms `ChestLockService#init`'s `loadCache()` ran at `@PostConstruct`) | server | |
| ultiessentials.back.not-persisted | `features.back.enabled: true`; the player has a recorded `/back` location (run `ultiessentials.back.track` first), and does NOT quit before the restart | Restart the server WITHOUT the player quitting first, then rejoin and run `/back` | Chat shows the same translated `No location to return to` line as `ultiessentials.back.teleport.neg-no-location` -- the location did not survive the JVM restart, confirming the map is truly in-memory-only rather than merely quit-cleared | server | |
| ultiessentials.hide.not-persisted | `features.hide.enabled: true`; the player is currently vanished (run `ultiessentials.hide.toggle` first), and does NOT quit before the restart | Restart the server WITHOUT the player quitting first, then rejoin | The player is fully visible to everyone on rejoin, with no `/hide` command run — vanish state did not survive the JVM restart | server | |

## Configuration

One row per `@ConfigEntity` class (D-06's config-per-file rule), not per key: `essentials.yml`
(59 keys), `spawn.yml` (8 keys), `lobby.yml` (6 keys), `motd.yml` (3 keys), `tabbar.yml` (2 keys)
— 78 keys total, matching `FEATURES.md`'s `## Configuration` section exactly. Each row confirms
every key in the file is present at its `FEATURES.md`-documented default, then flips one or more
representative keys and observes the behaviour follow — **except the four keys `FEATURES.md`
documents as having no observable effect** (`features.wild.cooldown`, `features.recall.enabled`,
`features.ban.broadcast-ban`, `features.ban.broadcast-unban`), which this section deliberately does
NOT attempt to exercise for an effect, per the same "do not test a key with no effect" convention
the framework's own `UAT-CHECKLIST.md` applies to `ultipanel.logging.batch.interval` and UltiChat's
own checklist applies to its three no-effect `announcements.*.interval` keys.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiessentials.config.essentials-yml | Fresh `config/essentials.yml` at its shipped default | Load the file; confirm all 59 keys listed under `FEATURES.md`'s `## Configuration` section are present at their documented defaults; then set `features.heal.enabled: false` (default true), restart, and confirm `ultiessentials.heal.self` (see `## Player Status & Utility`) now shows the translated `feature_disabled` line instead of healing; separately set `features.home.default-max-homes: 1` (default 3) and confirm a second `/sethome` attempt now hits the limit at count 1, not 3. Do NOT vary `features.wild.cooldown`, `features.recall.enabled`, `features.ban.broadcast-ban`, or `features.ban.broadcast-unban` expecting any observable effect — none has one (UltiKits/UltiEssentials#27) | All 59 keys present at their documented defaults before any change; after the changes, `/heal` is refused while disabled, and the home limit is enforced at the new, lower value | protocol | |
| ultiessentials.config.spawn-yml | Fresh `config/spawn.yml` at its shipped default | Load the file; confirm all 8 keys are present at their documented defaults; then set `spawn.location.x: 100` (default 0.0), restart, and confirm `/spawn` now teleports to X=100 rather than X=0 | All 8 keys present at their documented defaults before the change; after the change, `/spawn` teleports to the new X coordinate | server | |
| ultiessentials.config.lobby-yml | Fresh `config/lobby.yml` at its shipped default | Load the file; confirm all 6 keys are present at their documented defaults; then set `lobby.location.y: 100` (default 64.0), restart, and confirm `/lobby` now teleports to Y=100 | All 6 keys present at their documented defaults before the change; after the change, `/lobby` teleports to the new Y coordinate | server | |
| ultiessentials.config.motd-yml | Fresh `config/motd.yml` at its shipped default | Load the file; confirm all 3 keys are present at their documented defaults; then set `motd.line1` to a distinguishable value, restart, and ping the server's status | All 3 keys present at their documented defaults before the change; the server-list MOTD's first line shows the new, distinguishable value | os-input | |
| ultiessentials.config.tabbar-yml | Fresh `config/tabbar.yml` at its shipped default | Load the file; confirm both keys are present at their documented defaults; then set `tabbar.header` to a distinguishable value, restart, and rejoin | Both keys present at their documented defaults before the change; the joining player's tab-list header shows the new, distinguishable value | java-client | |
